# Handoff — SOW-to-Jira Elevation → per-agent Instructor migration

**Generated:** 2026-06-15
**Branch:** `elevation/wave-0` (HEAD: `4352624`, 20 commits ahead of `main`)
**Checkpoint commit:** `4352624` (the docs commit that landed this handoff)
**Linear:** [SOW-to-Jira Elevation](https://linear.app/calibraint-ai/project/sow-to-jira-elevation-883972f170ac) (CAL-113…126; team CAL)
**Status:** Harness + intelligence-quality work shipped and green (331 passed / 1 skipped). Instructor installed + `complete_structured` on the AgentRunner seam, but **no agent uses it yet** — that's the next mission. `main` untouched; nothing pushed.

## 1. What's done (all verified green before commit)

| Area | Status |
|------|--------|
| Wave 0 hardening (`/api/push` import, `.dockerignore`, CI, `$PORT`, pyproject, scripts moved) | ✅ `09a29d9` |
| Wave 3 quality slice (`complete_json` truncation→`LLMTruncationError`, critic gate, dedup merge/degraded, get_gaps, coverage gate) | ✅ `1596b0e` |
| Ports/adapters foundation (`core/results.py`, `core/ports.py`, `app/container.py`) | ✅ `3fa3daa` |
| AgentRunner seam — all 6 agents route through `self.runner.complete_json` | ✅ `d1698c5` |
| Guardrail band + RunHealthReport (`core/guardrails.py`, `core/health.py`) | ✅ `0e3f648` |
| Jira robustness (idempotency, 429/Retry-After backoff, `hierarchy_degraded`) | ✅ `cae9c6a` |
| Domain normalizing enums (`_NormalizedStrEnum`; DedupDecision/DependencyKind/VerifiedBy) | ✅ `3c13cc8` |
| Confidence clamp [0,1] + critic/coverage wired onto `ConfidenceGate` | ✅ `af9f334` |
| Instructor `1.15.1` installed + `AgentRunner.complete_structured` (additive, not wired into agents) | ✅ `93030df` |
| Bifrost+Langfuse design + locked decisions (docs only) | ✅ `e3d1904`, `1c91e8e` |
| Suite | `venv/bin/pytest tests/` → **331 passed, 1 skipped, EXIT 0** |

## 2. Branch state (oldest at bottom)
```
4352624 docs: session checkpoint 2026-06-15 — SOW-to-Jira Elevation, Instructor foundation
07bb7e4 docs(elevation): tracker — Instructor structured-output path shipped (331 tests)
93030df feat(harness): add Instructor-backed structured-output path to AgentRunner (additive)
d73b05c docs(elevation): tracker — confidence invariants + live guardrails
af9f334 feat(harness): clamp confidence to [0,1] + wire critic/coverage onto ConfidenceGate
0c42c81 docs(elevation): tracker — domain-model normalizing enums
3c13cc8 feat(schema): normalizing str-enums for closed-set fields (H-23, INV-1)
de1ed00 docs(elevation): tracker — Wave 5 Jira robustness
cae9c6a feat(jira): push idempotency, rate-limit backoff, degraded-hierarchy signal (TDD)
1c91e8e docs(elevation): lock Bifrost/Langfuse decisions
e3d1904 docs(elevation): add Bifrost gateway + Langfuse design
33340cf chore: gitignore graphify-out/
84839cb docs(elevation): tracker — guardrail band + health
0e3f648 feat(harness): guardrail primitives + per-run health report (additive)
3579de0 docs(elevation): add live UPGRADES.html elevation tracker
d1698c5 feat(harness): route all 6 agents through AgentRunner seam
3fa3daa feat(harness): ports-and-adapters seam foundation (strangler-fig)
1596b0e fix(wave-3): harness-quality fixes for the intelligence layer (TDD)
a60d6c6 docs(elevation): audit, architecture, harness study, render migration + plan
09a29d9 chore(wave-0): hardening foundation
```

## 3. The next session's mission — per-agent Instructor migration

Route each agent's LLM call from `self.runner.complete_json(...)` (regex-scraped JSON) onto `self.runner.complete_structured(prompt=..., response_model=<Model>, ...)` (Instructor-validated). Completes the full C-5 fix. **One agent per step, TDD, full suite green between each.**

Per agent: (a) ensure a Pydantic `response_model` exists for its output (critic=`TaskCritique`, coverage=`SectionCoverageReport`, classifier=`ClassificationResult` already exist; **extraction / deduplication / gap_recovery need models defined**); (b) swap the call to `complete_structured`; (c) the agent now receives a validated model — adapt its downstream use (was a dict); (d) **the agent's existing test mock-point moves** from `complete_json` to `complete_structured` — this is an *allowed* test edit (update the mock boundary, **preserve every behavioral assertion**); (e) `venv/bin/pytest tests/ -q -p no:warnings` green.

Suggested order (simplest first): **classifier → critic → coverage_check → extraction → gap_recovery → deduplication** (dedup last; it's the pairwise-loop special case). Verify each: `EXIT=0`, no assertion weakened.

`done` signal: all 6 agents call `complete_structured`; `complete_json` can stay as a fallback or be removed once unused; suite green; add a per-agent test asserting an Instructor `ValidationError`/`InstructorError` is surfaced (not swallowed).

## 4. Open behavioral risks
- **Test mock-point drift:** moving the stub from `complete_json`→`complete_structured` must keep the *same assertions*; only the patched boundary changes. Verify with `git diff` that no assertion line was weakened.
- **Real Instructor↔LiteLLM path is only stubbed in tests** (no network) — the live provider call is unexercised until a real run. Smoke-gate against a live provider before declaring C-5 fully closed.
- **Behavior change at the boundary:** `complete_structured` returns a validated model and raises `InstructorError` on failure, vs `complete_json`'s broad-except→empty. Agents must decide degrade-vs-fail consistently (prefer recording a `StageResult`/degraded signal over silent empty).
- **`max_tokens` sizing:** `complete_structured` defaults 8192; size per agent payload (dedup batches are large).

## 5. Explicitly NOT in scope (locked)
- **Do NOT touch the `BIFROST_*` / `os.environ` credential / `llm_router` paths** — the Bifrost design (BIFROST-LANGFUSE.md) owns those; reviving/deleting them here conflicts.
- **No platform work** (Postgres/Redis/Render/auth) — needs provisioned services; Waves 1/2/4 stay planned.
- **No Linear bulk edits** without user go-ahead (§7 guidebook). Milestones + §5 hygiene + ticket backfill are bundled into the **S2J team migration** (user creates the team in UI first).
- **No push / PR / deploy** without explicit go-ahead. Don't relitigate the locked Bifrost/Langfuse decisions (hybrid keys, US+masking).

## 6. Key files for the deep dive
- `core/agent_runner.py` — `complete_structured` (the target seam; added `93030df`) + `complete_json`/`run`.
- `core/agent_spec.py` — `AgentSpec` (prompt/response_model/policy).
- `pipeline/agents/{extraction,deduplication,critic,coverage_check,classifier,gap_recovery}.py` — the 6 agents to migrate; each builds `self.runner` in `__init__`.
- `models/schemas.py` — response models + `_NormalizedStrEnum` + `clamp_unit_interval`/`UnitInterval`.
- `core/guardrails.py`, `core/health.py` — gates + health (already wired).
- `tests/test_agent_runner_structured.py` — the stub pattern for the Instructor boundary (reuse it per agent).

## 7. Test data + how to verify
- Run suite: `venv/bin/pytest tests/ -q -p no:warnings` (331 passed / 1 skipped today). No network needed — all LLM/Instructor boundaries are stubbed.
- venv: `venv/bin/python` (3.11.15). `instructor==1.15.1`, `litellm`, `pydantic` v2 present.
- Per-agent tests: `tests/test_{critic,dedup,coverage_check,extraction,classifier,gap_recovery}.py`.

## 8. Documents to read in order
1. This handoff.
2. `.planning/elevation/UPGRADES.html` — live tracker (23 upgrades, 331 tests, 19 commits).
3. `.planning/elevation/IMPLEMENTATION-PLAN.md` — 169 change-ids + Patch v2 + Bifrost addendum + INV-1…B5.
4. `.planning/elevation/HARNESS-ARCHITECTURE.md` — the core-AI/harness split this all serves.
5. `.planning/elevation/BIFROST-LANGFUSE.md` — gateway design + locked decisions (NOT this session's scope).
6. Memory: `~/.claude/projects/-Users-magi-Documents-projects-sow-to-jira/memory/` (`linear-guidebook.md`, `elevation-render-saas-direction.md`, `keep-upgrades-tracker-updated.md`).

## 9. How to verify "done" next session
- [ ] All 6 agents call `complete_structured` (grep each agent file).
- [ ] Each agent has a defined `response_model`; extraction/dedup/gap_recovery models added.
- [ ] `venv/bin/pytest tests/` green (≥331 passed), no existing assertion weakened (`git diff` audit of test files).
- [ ] Each migrated agent surfaces `InstructorError`/validation failure as a recorded degraded/failed signal, not silent empty.
- [ ] `UPGRADES.html` + graphify refreshed; per-wave commit landed; (S2J migration still deferred to user).
