# HANDOFF — 2026-07-06 · Elevation R0→R4 DONE; next session = Argus decommission + live verification

**Branch:** `elevation/wave-0` · **~22 commits** `366dd2f`..`c2dac8f` on top of `2af28df`, **UNPUSHED** · **Green:** pytest **830/2**, vitest **96/96**, OTel-absent import smoke OK, render blueprint schema-valid (only `need_payment_info` billing).

## Done this session (the whole NEXT-SESSION-PLAN R0→R4)
R0 db-SQLite-shim + observability de-OTel · R1 auth/seams/DB-repos/gc/render/verify-telemetry-delete · R2 orchestrator-store-routing + server push-results + multi-tenant hardening (IDOR→404/CSRF/upload) · R3 push-chips + Test-Connection + role-aware Docker entrypoint + token scrubs · R4 server-OTel-guard + langfuse-fail-open + FS→PG migrator + **OTel dropped from requirements**. Every phase TDD + 3-skeptic adversarial review (default-reject). See `UPGRADES.html` (living doc, current) and per-commit messages.

## NEXT SESSION — same max-parallel method (multiple concurrent Workflows, per-phase TDD → adversarial-review → commit → UPGRADES → graphify)

### 1. PRIMARY (offline, fan out): FINAL WAVE — Argus decommission — spec is `NEXT-SESSION-PLAN.md §8`
Retire the self-hosted Argus observability fleet now that hosted uses Langfuse Cloud (OTel already gone in R4). Disjoint streams:
- **AR-R0 (2 concurrent):** `AR-OBS` (hot, one agent) — strip ARGUS collector paths + `INSTANCE_ID`/`argus.instance_id` from `pipeline/observability.py`, remove ONLY the `INSTANCE_ID` import/use from `pipeline/llm_client.py` (LOCKED-adjacent), fix `test_observability_shims.py`; keep Langfuse-cloud + de-OTel shims. ∥ `AR-INFRA-DELETE` — delete `infra/user/**` + `config/user/argus-collector-edge.yaml`.
- **AR-R1:** `AR-INSTALL-DOCS` — scrub ARGUS_* + `infra/` bring-up from `scripts/install/install.sh`; update `README.md`/`CLAUDE.md`/`AGENTS.md`/`GEMINI.md`. Leave `OLLAMA_*`.
- **AR-R2 (decisions + walled):** Bifrost scope (below) → maybe delete `infra/admin`/`bifrost.admin.yaml`; then the secret remediation. Verify gauntlet: pytest ∥ npm test ∥ OTel-absent smoke ∥ `git grep <token>`=0.
- Gates/conflict/LOCKED/walled: all in `§8`.

### 2. DECISIONS — LOCKED 2026-07-06/07 (no longer open)
- **a) Push — DONE.** Branch `elevation/wave-0` @ `eec9bba` pushed + verified on **both** remotes (GitHub origin PR #7 OPEN; GitLab `git@calib.dev`). Future pushes: gh active must be `magggiiii`; GitLab needs VPN; verify `git ls-remote`.
- **b) Bifrost scope — DECIDED: Argus-only.** Bifrost stays **LOCKED** — the Argus wave does NOT touch `infra/admin` or `config/admin/bifrost.admin.yaml`.
- **c) Token history — DECIDED: rotate, do NOT rewrite.** No `filter-repo`/force-push. ⚠️ **ACTION STILL OPEN (human/out-of-band):** rotate/invalidate `ARGUS_BACKBONE_TOKEN` in the Argus backend — it's scrubbed from all tracked files but remains in the (now pushed, public) history, so only rotation neutralizes it.

### 3. LIVE-SERVICE VERIFICATION (walled all run — needs provisioned services; not max-parallel offline)
Real Postgres migration run + INET/JSONB row assertions + audit→PG (1.6d); real arq/redis worker + route rewrite; Google OAuth token exchange (2.5d) + live sign-in smoke; real Render deploy (render.yaml provisioning needs the account payment method — the `need_payment_info` errors); live Langfuse trace; browser-smoke of FE-1/FE-2 (jsdom can't cover focus/DnD/responsive). Also deferred: OBS-tail `4.1c` telemetry allowlist.

## Method / reusable harness
Clone a review-harness template under `.claude/.../workflows/scripts/elevation-*.js` (impl → 3 skeptics → adjudicate → fix, with the `decide()` null-guard). Re-confirm green at session start. Run pytest as `PYTHONDONTWRITEBYTECODE=1 venv/bin/pytest <f> --junit-xml=/tmp/x.xml` + parse the XML (rtk eats summaries; **clear `__pycache__` first** — stale bytecode caused false-greens); diff-to-file+grep for LOCKED checks (rtk truncates `git diff` to stdout). A workflow result carrying a `<failures>` block (transient API / session-limit) is VACUOUS — verify directly (done for R2a when a usage limit hit mid-review).

## LOCKED (never touch)
`llm_router.py` / `configure_litellm_for_mode`; `BIFROST_*` + `bifrost.admin.yaml` routing; `data/.keyfile`; `os.environ` credential-WRITE paths (JIRA_*/LITELLM_*); `OLLAMA_*` / STEP 5.1.
