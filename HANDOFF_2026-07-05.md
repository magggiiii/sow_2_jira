# Handoff — Wave-5 fixes + STEP 3.8 remapper shipped & pushed (PR #7) → next: service/live-run walls

**Generated:** 2026-07-05 (continues `HANDOFF_2026-07-04.md`)
**Branch:** `elevation/wave-0` (HEAD ≈ `f366b0a`, 1 local commit ahead of the pushed `063cf5f`; run `git log -1` for the exact SHA — the checkpoint amend shifts it by one)
**Checkpoint commit:** `f366b0a` (the docs commit that landed this handoff — local-only, not pushed)
**Linear:** [SOW-to-Jira Elevation](https://linear.app/calibraint-ai/project/sow-to-jira-elevation-883972f170ac) (team CAL). **DEFERRED — no comment posted (locked).**
**Status:** **Offline elevation work is COMPLETE.** 4 increments shipped this session (Jira link direction, typed exceptions, STEP 3.8 dedup-UUID remapper, anchored retry classification), each TDD + adversarial-review (0 confirmed defects at commit). Suite **604 passed / 1 skipped, EXIT 0**. Branch **pushed to BOTH remotes** (`063cf5f`); **PR #7 updated** (100 commits, OPEN). Only the local checkpoint commit is unpushed. Remaining work is all behind the live-run / provisioned-service walls.

## 1. What's done
| Item | Status |
|------|--------|
| (a) Jira issue-link direction — explicit per-kind `LINK_DIRECTION` map | ✅ `4308e65` (+docs `0e6d95b`). "Inverted" claim was STALE: `blocks` was already correct (3 tests); the real bug was `duplicates` (was "target duplicates source"), now "source duplicates target". Review: 0 defects. |
| (b) Typed exception classification — `core/errors.py` | ✅ `868e95a` (+docs `2a1d264`). `ErrorClass{TRANSIENT,USER_FIXABLE,TERMINAL}` + `classify_exception` + `JiraPushError`; `error_class` field on `JiraPushResult` + `ProcessingStatus`, set at push + pipeline/server boundaries. Review found 1 defect (status-regex false-positive) → fixed + re-verified FIX_SOUND. |
| (c) STEP 3.8 replay-time dedup-UUID remapper | ✅ `0810c6f` (+docs `1a61136`). `RecordedTaskRef` + `build_dedup_id_remap` + `remap_recorded_dedup_ids` in `pipeline/evals/replay.py`. Review found 4 defects (collision→self-merge data loss, position-fallback wrong-merge, 2 test gaps) → all fixed; re-verify caught a first-come order-dependence → fixed via two-pass. 14 tests. |
| (d) Anchored retry classification (sibling of b) | ✅ `a9765dd` (+docs `063cf5f`). Same unanchored `\b(4\d\d\|5\d\d)\b` bug fixed in ALL 3 copies (`core.errors` from b, `pipeline/llm_client.py`, `pageindex/utils.py`). Re-verify FIX_SOUND. |
| Suite | ✅ 604 passed / 1 skipped, EXIT 0 (`venv/bin/pytest tests/ -q -p no:warnings`) |
| `make verify` | ✅ EXIT 0 (imports resolve) |
| Push GitHub (`magggiiii/sow_2_jira`) + GitLab (`git@calib.dev:mageswaran/sow_2_jira`, SSH) | ✅ both at `063cf5f` |
| PR #7 (`elevation/wave-0`→`main`) | ✅ OPEN, head `063cf5f`, 100 commits |
| Tracker `UPGRADES.html` | ✅ 40 upgrades / 604 tests / 46 commits; Wave 3 82%, Wave 5 58%, scale-hardening 90%; dated |

## 2. Branch state (newest first; since prior handoff `3dbd3c9`)
```
(HEAD) f366b0a docs: session checkpoint 2026-07-05  ← this commit (local-only; run git log -1)
063cf5f docs(elevation): mark anchored retry-classification fix shipped in tracker   ← pushed (both remotes)
a9765dd fix(retry): anchor status-in-message regex in llm_client + pageindex
1a61136 docs(elevation): mark dedup-UUID remapper (STEP 3.8) shipped in tracker
0810c6f feat(evals): replay-time dedup-UUID remapper (STEP 3.8 offline sub-component)
2a1d264 docs(elevation): mark typed exception classification shipped in tracker
868e95a feat(errors): typed error taxonomy classified at push + pipeline boundaries
0e6d95b docs(elevation): mark Jira link-direction (jira-8) shipped in tracker
4308e65 feat(jira): resolve dependency-link direction per-kind via LINK_DIRECTION (jira-8)
3dbd3c9 docs: session checkpoint 2026-07-04 — offline backlog shipped to remotes
```
Both remotes at `063cf5f`. The checkpoint commit above is local-only (skill: checkpoints stay local; push with go-ahead if desired — it only adds this handoff + memory pointer).

## 3. The next session's mission — the WALLS (need a live model or provisioned services)
No clearly-scoped OFFLINE work remains. Everything below needs setup and should be scoped with the user first.

1. **STEP 3.8 live-run faithful cassette (needs real gemini).** Wire the shipped remapper into a real recording: run `record_cassette` (`pipeline/evals/recorder.py`) against a real 103-node SOW, capture per-task `RecordedTaskRef`s (id+title+node_id+position) alongside the dedup entry, then apply `remap_recorded_dedup_ids` at replay in `EvalHarness`/`make_structured_replay`. **Note the recorder currently captures only `(agent, node_id, response)` — the refs need a capture path (from the dedup prompt or a small side-channel), which is the one open design question. The recorder must NOT change its raw-truth contract; add refs as a sidecar.** Gate: replayed cassette reproduces `merge_count ≥ 1`.
2. **STEP 3.7 final `run()` deletion** — gated on ①. Once the faithful cassette drives the band test as a required CI gate, flip `PipelineOrchestrator.run()` → `PipelineRunner` and delete the legacy path (equivalence golden already exists).
3. **Service Waves (need Postgres/Redis/Render/OAuth):** Wave 1 (Postgres/R2/secrets), Wave 2 (Redis worker/OAuth), Wave 4 (Render/observability), STEP 3.9 (pgvector). Designed behind seams; can't be verified offline. Scope per `IMPLEMENTATION-PLAN.md`.
4. **STEP 5.3 tail (verify first):** delete unwired MCP client + PushGate integration — believed already shipped; confirm before touching.

## 4. Open behavioral risks / gotchas
- **STEP 3.8 ref capture** is the real open problem (see §3.1). The remapper's matching (title→node→position, two-pass, collision-safe, self-merge-dropped) is unit-proven; the un-proven part is *where the refs come from* on a live recording.
- **`rtk` eats pytest's summary line + push output** — verify with `--junit-xml=/tmp/x.xml` + parse (`tests`/`failures`/`skipped`), or `git ls-remote <remote> refs/heads/elevation/wave-0` after a push. Trust EXIT=0 + counts, not dot output.
- **GitHub push account:** `gh` has TWO accounts; `magggiiii` (repo owner) MUST be active or pushes 403 as `Maaagggi`. This session it was already active. If 403: `gh auth switch --user magggiiii`. origin = `https://magggiiii@github.com/...`.
- **GitLab is SSH** (`git@calib.dev:…`, ed25519). `ssh -T git@calib.dev` greets `@mageswaran`.
- **macOS Documents permission:** if git/reads `EPERM`, grant Full Disk Access + restart terminal.
- **Leaked GitHub PAT** (`ghp_…`, dead + stripped) should still be **rotated**.

## 5. Things explicitly NOT in scope (locked)
- **STEP 5.1** — removing `LLMMode.LOCAL`/Ollama/Bifrost. **LOCKED. Do not.**
- Do NOT touch `pipeline/llm_router.py` / `BIFROST_*` / `os.environ` credential paths. (`pipeline/llm_client.py` IS editable — that's where the retry fix landed.)
- `gemini-2.5-flash` + `Mode.JSON` is the validated structured-output default.
- No push/PR/deploy without explicit go-ahead. **Linear deferred (no comment).**
- The status regex is now anchored in all 3 copies — keep them in lockstep if edited.

## 6. Key files
- `core/errors.py` — `ErrorClass` + `classify_exception` + `JiraPushError` + `_STATUS_IN_MESSAGE` (canonical anchored regex).
- `integrations/jira_client.py` — `LINK_DIRECTION` map (~line 30); `error_class` on the 3 push-failure returns.
- `ui/server.py` — `ProcessingStatus.error_class`; classify at `run_pipeline_task` (~300) + `run_push_task` (~745/747).
- `pipeline/evals/replay.py` — the STEP 3.8 remapper (`RecordedTaskRef`, `build_dedup_id_remap`, `remap_recorded_dedup_ids`).
- `pipeline/evals/recorder.py` / `harness.py` — where the live-run ref-capture (§3.1) must be wired.
- `pipeline/llm_client.py` / `pageindex/utils.py` — the two ported `_STATUS_IN_MESSAGE` copies.
- `.planning/elevation/UPGRADES.html` (living tracker) + `IMPLEMENTATION-PLAN.md` (change-ids) + `AUDIT.md`.

## 7. Test data + how to verify
- Suite: `venv/bin/pytest tests/ -q -p no:warnings` → 604 passed / 1 skipped (offline; LLM stubbed/replayed). Use `--junit-xml` + parse for exact counts.
- New tests this session: `tests/test_errors.py` (27), `tests/test_error_boundaries.py` (3), `tests/test_dedup_remap.py` (14), + additions to `test_jira_client.py` / `test_phase2_runtime_reliability.py`.
- venv: `venv/bin/python` (3.11.15). `make verify` for the import smoke test.

## 8. Documents to read in order
1. This handoff. 2. `HANDOFF_2026-07-04.md` (prior session — offline backlog + push/PR setup). 3. `.planning/elevation/UPGRADES.html` ("Next up"/"Planned") + `IMPLEMENTATION-PLAN.md` (STEP 3.7/3.8, Wave-5 change-ids). 4. Memory: `elevation-progress`, `inv4-cassette-dedup-uuid`, `keep-upgrades-tracker-updated`, `graphify-refresh-method`.

## 9. How to verify "done" next session
- [ ] Direction confirmed with user before any live-run/service work (all remaining is behind the walls).
- [ ] If STEP 3.8 live: `RecordedTaskRef` capture path added WITHOUT changing the recorder's raw-truth contract; faithful cassette replays `merge_count ≥ 1`; suite green.
- [ ] `UPGRADES.html` updated + `graphify update .` after each increment (user directive).
- [ ] Suite ≥ 604 green; adversarial-review → 0 confirmed defects per increment; nothing pushed without go-ahead; Linear deferred; STEP 5.1 / llm_router / BIFROST untouched.

---

## SESSION UPDATE — part 2 (2026-07-05): pushed + NEW WAVE 6 (UI/UX rework) — now the next offline wave

**This supersedes §3's "walls-only" mission.** After the 4 offline increments landed, this part of the session:
- **Pushed** `elevation/wave-0` to both remotes and **PR #7 is updated** (head `063cf5f`, OPEN, 100 commits) — done with explicit user go-ahead.
- Refreshed the tracker header (`cc9f626`) — date 2026-07-05, 604 tests, branch pushed.
- **Added WAVE 6 — UI/UX rework** (`4fec8dc`): a 4-lens frontend audit (IA · visual · UX/a11y · code) + synthesis + adversarial critique, written up as `## WAVE 6` in `IMPLEMENTATION-PLAN.md` (steps **UI.1→UI.7**, change-ids `ui-1..24`) + a tracker wave-bar ("Queued") + §02 Next-up card.

**New commits since the checkpoint (`6b15803`):** `cc9f626` (tracker header), `4fec8dc` (WAVE 6). All local + the checkpoint are local-only; the pushed remote head is `063cf5f`.

### The next mission = WAVE 6, and it IS offline-buildable (unlike the §3 walls)
Audit blockers to fix first: (1) the **theme toggle switches to a light theme that doesn't exist** → app breaks; (2) the new **`error_class` taxonomy is completely unconsumed** → every failure dead-ends at "Dismiss". Plus: 1 `@media` in 619 lines (not responsive), no modal/keyboard a11y, a 979-line IIFE god-file, ACs edited as a free textarea (won't round-trip structured `AcceptanceCriterion`).

**Start with STEP UI.1** (S, no deps, offline): delete the dead theme toggle + light path; make tokens honest (`--surface-2` fix, extend type scale, `--status-*`/`--sev-*` tokens, a color for every `TaskStatus` dot); polling-lifecycle hardening (double-start guard, await-cancel, stale-response guard, backoff); quick correctness (persistent error toasts, empty-state-flash guard, drop the wrong `/6` denominator, neutral overlay title). Then UI.2a (Vite seam) → UI.2b (module split + nanostores) → UI.3 (error_class UX) → UI.4a/b (review flow) → UI.5 (flow integrity) → UI.6 (responsive + modal a11y, parallelizable) → UI.7 (onboarding + push-results + style cleanup). Full spec: `IMPLEMENTATION-PLAN.md` WAVE 6.

### Open methodology fork for the next agent (resolve with the user first)
**There is NO frontend test harness** (the pytest suite is backend-only) — classic RED→GREEN TDD doesn't apply to `ui/*.js|html|css` until UI.2 adds Vite (which could bring vitest). So decide up-front how to verify UI work: (a) add a tiny JS test runner as part of UI.2a and TDD the JS-logic bits, and/or (b) verify visually by running the app (`make ui`) + browser check (the `verify`/`run` skills or a browser MCP) for CSS/markup/a11y. UI.1 is mostly deletions + CSS tokens + small JS guards → run-the-app verification is reasonable; the polling-logic guards (ui-3) are the part that most wants a real test.

### Wave-6 locked calls (from the audit critique)
- **Keep-vanilla + light build** (Vite + nanostores, NO JSX framework) — right-sized for a solo local-first ~6-endpoint single-screen tool.
- Commit to the **single dark theme** (delete the light aspiration); single-accent orange; error-severity chips; **status never color-only** (WCAG 1.4.1).
- UI.3 backend truth: `/api/push` returns async and **never** emits `error_class` synchronously (`server.py:762-777`); `ProcessingStatus` has **no** `total_steps` field — push error_class is handled in the shared POLLING branch.
- Per CLAUDE.md, kick off each UI.x through a GSD command so planning artifacts stay in sync.

---

## SESSION UPDATE — part 3 (2026-07-05): UI.1 + UI.2a shipped → NEXT SESSION runs WAVE-6 phases MAX-PARALLEL

**Shipped this part (all local, not pushed; remote head still `063cf5f`):**
- **UI.1** (`32ba3f4` + tracker `ea001d7`): honest tokens (Astryx conventions, copied no-dep), single dark theme (deleted the broken toggle), polling/UX hotfixes. Run-app + browser verified; adversarial review 1 defect → fixed.
- **UI.2a** (`baaa3d0` + tracker `58db402`): **Vite 8 build seam**. `package.json`+`vite.config.js` (root=ui, base=/static/, build→ui/dist, dev proxy /api→:8000); `package-lock.json` committed (repo had no lockfile); `node_modules`+`ui/dist` gitignored. `ui/index.html` refs → Vite-processable (module `/app.js`, `/styles.css`); **app.js still a monolithic IIFE** (split is UI.2b). `ui/server.py` serves `ui/dist` when built else raw `ui/`. Makefile: `ui`=build+serve, `ui-dev`=Vite HMR+API, `install`+=`npm install`. Dockerfile: node build stage → overlays `ui/dist`.
  - **Verified:** `npm run build` → dist; uvicorn serves built index + hashed assets (200); browser loads bundled app, **no JS console errors** (only benign favicon 404).
  - **NOT verified (flag):** the Docker **image build** (`docker build`) — the node stage is authored but unbuilt here. Verify with a `docker build` pass. Also **vitest was NOT added yet** — add it as the FIRST task of UI.2b (Vite→vitest is ~free) so the module split + all later phases get real RED→GREEN tests (the hybrid-verification decision).

### NEXT SESSION MISSION — implement the MAXIMUM number of non-dependent WAVE-6 phases in PARALLEL (user directive)
Use **ultracode + a Workflow per phase**, **worktree-isolated** (`isolation:'worktree'`) so parallel phases don't collide on `ui/*` files; merge between rounds; each phase = full cadence (add/za vitest tests where logic-bearing → RED→GREEN → adversarial-review workflow → commit → update `UPGRADES.html` → `graphify update .`). WAVE-6 dependency DAG (UI.1 ✅, UI.2a ✅):

- **Round 1 (do first, solo — it restructures all the JS):** **UI.2b** — split the 979-line IIFE into ES modules (`ui/src/{state,api,render,modals,polling}.js`) + a ~1KB nanostores store + API adapter; **add vitest first**. Everything else rebases on this module layout, so it can't run parallel with the others.
- **Round 2 (after UI.2b — run these 3 IN PARALLEL, worktree each):** **UI.3** (error_class-aware failure UX — deps UI.2b), **UI.4a** (review structural core: optimistic render + a11y collapsibles + XSS escape — deps UI.2b), **UI.6** (responsive + modal a11y — deps UI.1, CSS/markup-heavy, lowest conflict).
- **Round 3 (run these 3 IN PARALLEL):** **UI.5** (flow-integrity guards — deps UI.3), **UI.4b** (keyboard triage + filter/stat reconcile — deps UI.4a), **UI.7** (onboarding + push-results panel + style cleanup — deps UI.4a).

Max concurrency = **3 phases at once** (rounds 2 and 3). UI.3 & UI.5 have live-backend verify passes; the rest are fully offline. Full per-phase spec + change-ids (`ui-6..24`): `IMPLEMENTATION-PLAN.md` WAVE 6.

### SECOND, FULLY-PARALLEL track — WAVE 1 offline backend slices (run CONCURRENTLY with the WAVE 6 frontend track)
These are pure-Python (`config/`/`models/`/`core/`/`integrations/`), **zero file overlap with `ui/`**, and testable with the EXISTING pytest suite (real TDD, unlike the frontend) — so they run as a *separate concurrent stream* the whole time, not gated on any UI phase. Scoped 2026-07-05 against the codebase; all UNLOCKED (no `llm_router`/`BIFROST_*`/`os.environ`-credential/keyfile edits). Status → do the UNDONE/PARTIAL ones:
- **1.1** `config/crypto.py` (MultiFernet + rotation) + `tests/test_crypto.py` — UNDONE, isolated, do first.
- **1.3** tz-aware datetimes (`models/schemas.py` naive `datetime.utcnow()` at ~319-320/384 → tz-aware) + centralize intelligence/coverage schemas — PARTIAL. **Watch:** tz change can shift serialization/golden output — re-run the suite.
- **1.5** full-UUID run identity (`models/schemas.py:354` `uuid4()[:8]` → full uuid; add `core/domain/ids.py::make_run_id()`) — PARTIAL. **Watch:** `run_id` also names `data/sessions/<id>/` dirs in `ui/server.py` — keep back-compat.
- **1.4** `integrations/object_store.py` ObjectStore Protocol + `LocalObjectStore` shim (+ port in `core/ports.py`) — UNDONE, offline.
- **1.2** ORM models + Alembic baseline (author `pipeline/db.py` + `alembic/` migration files; do NOT run migrations — needs Postgres) — UNDONE, author-only offline.
- **1.7** Makefile `migrate`/`worker`/`lock`/`test` targets + refresh `verify` import list — PARTIAL.
- **Order:** 1.1 → (1.3 ∥ 1.5 ∥ 1.4 — independent files) → 1.2 (author) → 1.7. Each: pytest RED→GREEN → adversarial-review → commit → UPGRADES.html (bump **Wave 1** bar) → graphify.
- **Skip (still blocked/deferred):** 1.6 (persistence seam — needs 1.2-1.5 first + edits orchestrator), 2.x router/auth (LOCKED/service), 4.x (Render), 3.7/3.8/3.9 (live/pgvector).

**So next session has TWO concurrent streams: WAVE 6 (frontend, vitest/browser) ∥ WAVE 1 offline (backend, pytest).** They never touch the same files.

**Locked calls (unchanged):** keep-vanilla + light build (Vite ✅ + nanostores, no JSX); single dark theme; error-severity chips consume `--sev-*` (already in `:root` from UI.1); status-never-color-only; `/api/push` never emits `error_class` synchronously (polling branch only); no push without go-ahead; Linear deferred.

---

## SESSION UPDATE — part 4 (2026-07-05): BOTH streams delivered — WAVE 6 complete (9/9) + WAVE 1 offline complete (6 slices)

Ran the two concurrent non-conflicting streams to completion (ultracode + parallel background agents + a reusable adversarial-review Workflow). **All committed to `elevation/wave-0`, UNPUSHED (remote still `063cf5f`; PR #7 OPEN).** 10 commits this session.

**Green (final):** pytest **667 passed / 2 skipped** (EXIT 0), vitest **76/76**, `make verify` green, `npm run build` clean, **`docker build` succeeds** (the flagged UI.2a image verification — RESOLVED; image `sow-to-jira:test`, 9.47GB w/ ML deps).

**Commits (newest first):**
```
1c4dc66 docs(elevation): WAVE 6 complete (9/9) + Docker image verified
a1f3af6 feat(ui): triage velocity + flow integrity + onboarding/push-results (W6 UI.4b/UI.5/UI.7)
6bcdc03 docs(elevation): tracker — W1 offline data platform + W6 Round 2 shipped
bd22a44 feat(ui): error_class UX + a11y/XSS review core + responsive & modal a11y (W6 UI.3/UI.4a/UI.6)
ddb60c2 chore(make): test/migrate/worker/lock targets + refresh verify (W1 1.7)
2db2296 feat(db): async engine + ORM models + Alembic baseline (W1 1.2, author-only)
cae9d9c feat(ids): full-UUID run identity via core/domain/ids.make_run_id (W1 1.5)
2d194d9 feat(schemas): tz-aware UTC datetimes across the pipeline (W1 1.3)
b836de9 feat(store): hardened LocalObjectStore + consolidate composition root (W1 1.4)
118653b refactor(ui): split app.js IIFE into ES modules + nanostores store + vitest (W6 UI.2b)
99ccec5 feat(crypto): env-driven MultiFernet secret encryption + rotation (W1 1.1)
```

**Adversarial review earned its keep** — a real defect every UI round (the unit tests missed them because fixtures were fresh): Round2 stale `#btnDismissProgress` listener → silent data-loss on success-after-failure; Round3 (a) resume paths didn't set `lastOp` → push-failure Retry re-extracted, (b) fallback clicked a DISABLED button → silent no-op, (c) unvalidated Jira-server scheme → `javascript:` XSS in the results link. All fixed + regression-tested before landing. Also caught 1.4's LIVE symlink escape in the wired `app/container.py` copy. **Stale-plan corrections:** descoped 1.3's schema-centralization half (module-local + UnitInterval by design), and 1.4 consolidated a second wired object-store copy the plan didn't mention.

**Next session — full max-parallel plan authored: `.planning/elevation/NEXT-SESSION-PLAN.md`** (6 streams · 5 rounds · multi-workflow orchestration · conflict map · per-phase verification · walled/locked split · DAG — from a 7-agent planning workflow, verified against live code). Keystones it surfaced: **W2-S0 SQLite-compat shim is the gate for ALL offline DB tests** (`db.py` can't `create_all` on SQLite — INET/JSONB/pgcrypto); `push_results` is built at `server.py:727` but **discarded at :737** (BE-1 = persist it); `AuditLogger(run_id)` is latently broken vs `run_eval_dataset.py:31`; rotate the leaked `ARGUS_BACKBONE_TOKEN` at `install.sh:18`. Highlights below; full detail in that doc:
1. **Live-verify + browser smoke** — UI.3/UI.5 against real transient/user-fixable/terminal failures; a real-browser pass of responsive/focus/DnD/a11y (jsdom can't cover layout/focus/drag).
2. **WAVE 6 deferrals** — per-task push-failure chips (needs `server.py::run_push_task` to persist the `results[]` list, not just aggregate `first_error`) + a Test-connection endpoint+button (ui-24 nice-to-have).
3. **WAVE 1 STEP 1.6** — route orchestrator persistence through ObjectStore + DB write-through; then run `alembic upgrade head` + the skipped live-PG tests (needs a provisioned Postgres). psycopg2 not in venv — add `psycopg2-binary` for the sync migration driver.
4. Service waves 2/4 + 3.9 pgvector (Postgres/Redis/Render/OAuth); prior-wave walls (STEP 3.8 live cassette, 3.7 run() deletion) unchanged. STEP 5.1 / llm_router / BIFROST / os.environ-cred / keyfile LOCKED.
5. **Push decision** — 10 local commits ahead of `063cf5f`; needs explicit go-ahead. Push gotchas: `gh` active must be `magggiiii`; gitlab SSH `git@calib.dev`; verify with `git ls-remote`; rtk eats pytest summary + push output.
