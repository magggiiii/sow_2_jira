---
phase: 09-argus-optout
task: 1
total_tasks: 2
status: paused
last_updated: 2026-04-02T16:25:00.000Z
---

<current_state>
We have finished the "Argus Overhaul" (Phase 07) and "User vs. Admin Container Separation" (Phase 08). 
We are now starting **Phase 09: Argus Opt-Out & Local JSON Audit**.
The goal is to turn off remote sync by default and ensure that local JSON logging (`audit.jsonl`) is the permanent source of truth for manual log collection.
The plan has been discussed and approved but not yet implemented.
</current_state>

<completed_work>
- Milestone v1.0 fully stabilized, distributed, and overhauled with Argus HQ.
- Architecture for "Local-First" logging designed: permanent JSON sink + default-off remote sync.
- Phase 09 Implementation Plan drafted (`09-01-PLAN.md`).
</completed_work>

<remaining_work>
- Task 1: Implement Local JSON Audit & Opt-Out logic in `pipeline/observability.py`.
- Task 1: Update `pipeline/llm_client.py` to respect the `SYNC_ENABLED` toggle.
- Task 2: Update `install.sh` to set `ARGUS_SYNC_ENABLED=false` by default.
</remaining_work>

<decisions_made>
- **Local-First:** `audit.jsonl` will be written regardless of whether Argus or Bifrost are enabled.
- **Opt-Out Default:** `ARGUS_SYNC_ENABLED` will default to `false` in the installer.
- **Zero-Break Handshake:** If Argus is off, OTel SDK initialization will be skipped entirely to avoid connection errors or lag.
</decisions_made>

<blockers>
- None.
</blockers>

<context>
The user wants to ensure the system gracefully continues to work even if Bifrost isn't set up. The primary way to collect logs from users in the immediate future will be manual collection of the `audit.jsonl` file.
</context>

<next_action>
Execute Task 1 of the Phase 09 plan: Implement the permanent JSON sink and conditional OTel initialization in `pipeline/observability.py`.
</next_action>
