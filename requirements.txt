opendataloader-pdf
fastapi
uvicorn
gunicorn>=21.2.0
jira>=3.8.0
python-dotenv>=1.0.0
cryptography>=42.0.0
sentence-transformers>=2.7.0
scikit-learn>=1.3
requests>=2.31.0
rich>=13.7.0
numpy>=1.26.0
openai>=2.8.0
pydantic>=2.0.0,<3.0.0
pyyaml>=6.0
python-multipart
loguru>=0.7.0
httpx>=0.27.0
pytest-asyncio>=0.23.0
# PageIndex dependencies
litellm>=1.82.0
pymupdf>=1.26.0
PyPDF2>=3.0.1
pytest>=8.0.0
instructor==1.15.1
# Langfuse fail-open LLM tracing (WAVE 4, 4.1d) — optional at runtime; the
# llm_client wrapper degrades to a no-op when it's absent/unconfigured.
langfuse>=4.0
# Async DB engine + ORM + migrations (elevation WAVE 1, STEP 1.2)
sqlalchemy>=2.0
alembic>=1.13
asyncpg>=0.29
# SQLite driver + greenlet bridge for offline async ORM tests (WAVE 2, W2-S0)
aiosqlite>=0.19
greenlet>=3.0
