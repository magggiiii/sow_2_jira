# pipeline/features/ — feature-stage components that may depend on numpy / embedders.
#
# Kept OUT of numpy-free core/ (per IMPLEMENTATION-PLAN.md STEP 3.5: "the
# embedding-tier filter lives in the feature stage, not in numpy-free core").
