---
phase: 01-provider-consistency
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [pipeline/orchestrator.py, ui/server.py]
autonomous: true
gap_closure: true
requirements: [PROV-04]
must_haves:
  truths:
    - "Concurrent pipeline runs use independent tree cache files"
    - "Server does not leak Loguru handlers after runs complete"
  artifacts:
    - path: "pipeline/orchestrator.py"
      provides: "Run-specific document tree caching"
    - path: "ui/server.py"
      provides: "Lifecycle management for per-run loggers"
  key_links:
    - from: "pipeline/orchestrator.py"
      to: "data/sessions/{run_id}/document_tree.json"
      via: "Run-specific path"
---

<objective>
Close backend gaps preventing reliable concurrent extractions.
1. Refactor Orchestrator to use run-specific tree cache paths to avoid write collisions.
2. Implement explicit cleanup for per-run Loguru handlers in the UI server.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@pipeline/orchestrator.py
@ui/server.py
@pipeline/observability.py
@.planning/phases/01-provider-consistency/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Run-Specific Tree Cache</name>
  <files>pipeline/orchestrator.py</files>
  <action>
    Modify `PipelineOrchestrator._build_or_load_tree` to use a run-specific path for the document tree cache.
    
    Instead of the global `TREE_CACHE_PATH`, use:
    `Path(f"data/sessions/{self.config.run_id}/document_tree.json")`
    
    Ensure the parent directory is created before writing.
  </action>
  <verify>
    <automated>grep "data/sessions/.*document_tree.json" pipeline/orchestrator.py</automated>
  </verify>
  <done>Concurrent runs no longer collide on the document tree cache file.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup Per-Run Loggers in UI Server</name>
  <files>ui/server.py</files>
  <action>
    Update `run_pipeline_task` in `ui/server.py` to store and then remove the Loguru handler ID.
    
    1. Capture `logger_id = add_run_file_logger(run_id)`.
    2. In the `finally` block of `run_pipeline_task`, call `logger.remove(logger_id)` to detach the per-run file logger.
    3. (Optional but recommended) Do the same for `run_push_task` if it's updated to use per-run logging.
    
    Note: You will need to import `logger` from `loguru`.
  </action>
  <verify>
    <automated>grep "logger.remove(logger_id)" ui/server.py</automated>
  </verify>
  <done>Loguru handlers are correctly closed after run completion, preventing memory and file handle leaks.</done>
</task>

</tasks>

<success_criteria>
1. Backend supports multiple simultaneous extraction runs without file locks or status bleed.
2. System logs show handlers being removed at the end of each run.
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-consistency/01-05-SUMMARY.md`
</output>
