---
phase: 02-runtime-logging-reliability
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: ["pipeline/llm_client.py", "pageindex/utils.py", "pipeline/orchestrator.py", "pipeline/indexer.py", "tests/test_phase2_runtime_reliability.py"]
autonomous: true
requirements: ["PIPE-01", "PIPE-02", "PIPE-03"]
must_haves:
  truths:
    - Local Ollama runs do not terminate just because response time is long
    - Users can see explicit messaging that waiting is due to local Ollama/model/hardware speed
    - User cancellation interrupts long local waits quickly and cleanly
    - Remote/API provider retry limits remain bounded and unchanged
  artifacts:
    - path: "pipeline/llm_client.py"
      provides: "Provider-aware retry strategy with unlimited local-wait mode + cancellation-safe behavior"
      contains: "Local Ollama mode: unlimited wait"
    - path: "pageindex/utils.py"
      provides: "PageIndex local Ollama wait behavior with explicit status messaging and cancel-aware wait loop"
      contains: "Waiting for local Ollama model response"
    - path: "tests/test_phase2_runtime_reliability.py"
      provides: "Regression tests for local wait policy + cancellation + message clarity"
      contains: "test_local_ollama_wait_does_not_timeout"
  key_links:
    - from: "pipeline/orchestrator.py"
      to: "pipeline/llm_client.py"
      via: "Run summary + LLM execution path for local mode transparency"
      pattern: "Local Ollama mode: unlimited wait enabled"
    - from: "pipeline/indexer.py"
      to: "pageindex/utils.py"
      via: "PageIndex callback/status flow with local wait messaging"
      pattern: "Waiting for local Ollama model response"
---

<objective>
Implement local-Ollama-first runtime behavior:
1) wait as long as needed for local model responses,
2) continuously communicate to the user that waiting is due to local Ollama/model speed,
3) keep cancel responsive,
4) preserve bounded retry behavior for non-local providers.

Purpose: remove false failure behavior for slow local models while keeping reliability and user trust.
Output: updated runtime policy + status messaging + regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-runtime-logging-reliability/02-RESEARCH.md
@.planning/phases/02-runtime-logging-reliability/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider-Aware Wait Policy (Unlimited for Local Ollama)</name>
  <read_first>pipeline/llm_client.py, pageindex/utils.py</read_first>
  <files>pipeline/llm_client.py, pageindex/utils.py</files>
  <action>
    1. In `pipeline/llm_client.py`, split behavior by provider/mode:
       - Local Ollama path: no attempt-cap failure due to slow response time (wait/retry until success, cancellation, or non-retryable config/auth error).
       - Remote/API path: keep bounded attempts and backoff.
    2. In `pageindex/utils.py`, apply the same local-vs-remote policy for `llm_completion` and `llm_acompletion`.
    3. Keep non-retryable errors (`401/403/auth/provider misconfig`) as fast-fail in both paths.
  </action>
  <acceptance_criteria>
    - Local mode no longer throws max-attempt failure during prolonged Ollama response wait
    - API mode still respects bounded retry policy
    - Non-retryable auth/config errors still fail fast
  </acceptance_criteria>
  <verify>
    <automated>rg -n "ollama|local.*unlimited|non-retryable" pipeline/llm_client.py pageindex/utils.py</automated>
  </verify>
  <done>Local Ollama wait policy no longer times out due to arbitrary retry caps.</done>
</task>

<task type="auto">
  <name>Task 2: User Transparency Messaging for Long Local Waits</name>
  <read_first>pipeline/llm_client.py, pageindex/utils.py, pipeline/orchestrator.py, pipeline/indexer.py</read_first>
  <files>pipeline/llm_client.py, pageindex/utils.py, pipeline/orchestrator.py, pipeline/indexer.py</files>
  <action>
    1. Add explicit status/log message for local waits:
       "Waiting for local Ollama model response. This can take several minutes depending on model size and your machine."
    2. Add low-noise heartbeat updates (elapsed time + model) while waiting in local mode.
    3. Add exactly one run-summary line for local mode:
       "Local Ollama mode: unlimited wait enabled; cancel anytime."
    4. Ensure optional callbacks/loggers remain guarded (`if status_callback`, `logger or logger_global`) to preserve PIPE-03.
  </action>
  <acceptance_criteria>
    - Local runs show explicit “waiting on local Ollama” messaging
    - Run summary still prints once and includes local wait policy line
    - No logger/callback null crashes introduced
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Waiting for local Ollama model response|Local Ollama mode: unlimited wait enabled" pipeline/llm_client.py pageindex/utils.py pipeline/orchestrator.py</automated>
  </verify>
  <done>Users are explicitly informed why long local waits occur.</done>
</task>

<task type="auto">
  <name>Task 3: Cancellation Responsiveness for Indefinite Local Wait</name>
  <read_first>pipeline/llm_client.py, pageindex/utils.py</read_first>
  <files>pipeline/llm_client.py, pageindex/utils.py</files>
  <action>
    1. Ensure cancel checks run:
       - before request attempts,
       - during backoff sleep (slice-based sleep with stop_event checks),
       - and before retry re-entry.
    2. Ensure cancellation exceptions are terminal (not re-retried).
  </action>
  <acceptance_criteria>
    - Cancelling a local wait exits quickly
    - Cancel path does not loop back into retry
  </acceptance_criteria>
  <verify>
    <automated>rg -n "stop_event|cancel" pipeline/llm_client.py pageindex/utils.py</automated>
  </verify>
  <done>Unlimited local wait remains user-controllable through fast cancellation.</done>
</task>

<task type="auto">
  <name>Task 4: Regression Tests for Local Wait Policy</name>
  <read_first>tests/</read_first>
  <files>tests/test_phase2_runtime_reliability.py</files>
  <action>
    1. Add/extend tests covering:
       - local Ollama wait does not fail due to attempt/time budget exhaustion,
       - cancellation interrupts long local wait promptly,
       - transparency message appears in status output.
    2. Run:
       - `python3 -m pytest tests/test_phase2_runtime_reliability.py -q`
       - `python3 -m pytest -q`
  </action>
  <acceptance_criteria>
    - New tests exist and pass for local wait/cancel/message behavior
    - Existing tests remain green
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_phase2_runtime_reliability.py -q</automated>
  </verify>
  <done>Behavior is locked by tests and safe against regressions.</done>
</task>

</tasks>

<verification>
Validate a real local run with a deliberately slow Ollama model:
1. Start extraction in local mode.
2. Confirm UI/log repeatedly states wait is due to local Ollama speed.
3. Confirm run does not abort solely due to wait duration.
4. Trigger cancel and confirm prompt termination.
5. Run API-mode smoke test to confirm bounded retry behavior is unchanged.
</verification>

<success_criteria>
1. Local Ollama extraction no longer fails due to attempt/time cap.
2. User sees clear and repeated reason for long waits.
3. Cancellation remains fast and deterministic.
4. PIPE-01/02/03 remain satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-logging-reliability/02-02-SUMMARY.md`.
</output>
