---
phase: 11-evals-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config/admin/bifrost.admin.yaml, infra/admin/docker-compose.admin.yml, infra/admin/evaluator/Dockerfile, infra/admin/evaluator/requirements.txt, infra/admin/evaluator/main.py]
autonomous: true
requirements: [EVAL-03]
user_setup:
  - service: ollama
    why: "Local LLM-as-a-judge evaluations"
    dashboard_config:
      - task: "Ensure Ollama is running on host and bound to 0.0.0.0:11434"
        location: "Host machine"

must_haves:
  truths:
    - "Admin Bifrost can route requests to host Ollama"
    - "Evaluator container starts and stays running in admin stack"
    - "Evaluator can reach Langfuse and Bifrost via internal network"
  artifacts:
    - path: "config/admin/bifrost.admin.yaml"
      provides: "Ollama provider configuration"
    - path: "infra/admin/evaluator/Dockerfile"
      provides: "Python environment for evals"
    - path: "infra/admin/evaluator/main.py"
      provides: "Evaluator entrypoint"
  key_links:
    - from: "s2j-admin-evaluator"
      to: "s2j-admin-bifrost:8081"
      via: "HTTP"
    - from: "s2j-admin-evaluator"
      to: "s2j-admin-langfuse:3000"
      via: "HTTP"
---

<objective>
Establish the infrastructure for offline evaluations in the admin stack. This includes configuring local model access via Bifrost and standing up a dedicated evaluator container that can communicate with Langfuse and the LLM proxy.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-evals-architecture/11-CONTEXT.md
@.planning/phases/11-evals-architecture/11-RESEARCH.md
@.planning/phases/11-evals-architecture/REVIEWS.md
@infra/admin/docker-compose.admin.yml
@config/admin/bifrost.admin.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Admin Bifrost for Local Ollama</name>
  <files>config/admin/bifrost.admin.yaml, infra/admin/docker-compose.admin.yml</files>
  <read_first>
    - infra/admin/docker-compose.admin.yml (to check network names)
    - config/admin/bifrost.admin.yaml (to check provider structure)
  </read_first>
  <action>
    1. Update `config/admin/bifrost.admin.yaml` to add an `ollama-local` provider pointing to `http://host.docker.internal:11434/v1`.
    2. Add a virtual key for the evaluator that routes to `ollama-local`.
    3. Update `infra/admin/docker-compose.admin.yml` for the `bifrost` service to include `extra_hosts: ["host.docker.internal:host-gateway"]` ensuring it can reach the host's Ollama.
  </action>
  <verify>
    <automated>docker compose -f infra/admin/docker-compose.admin.yml exec -it bifrost curl http://host.docker.internal:11434/api/tags</automated>
  </verify>
  <acceptance_criteria>
    - Bifrost config contains ollama-local provider.
    - Bifrost container can resolve host.docker.internal and ping Ollama.
  </acceptance_criteria>
  <done>Bifrost configured to proxy local Ollama for evaluations</done>
</task>

<task type="auto">
  <name>Task 2: Create Evaluator Service Scaffold</name>
  <files>infra/admin/evaluator/Dockerfile, infra/admin/evaluator/requirements.txt, infra/admin/evaluator/main.py</files>
  <action>
    1. Create `infra/admin/evaluator/Dockerfile` using `python:3.11-slim`.
    2. Create `infra/admin/evaluator/requirements.txt` with `langfuse`, `langchain`, `langchain-openai`, and `requests`.
    3. Create `infra/admin/evaluator/main.py` that initializes the `Langfuse` client and performs a simple connectivity check to Langfuse and Bifrost. It should enter a non-blocking sleep loop to stay alive.
  </action>
  <verify>
    <automated>ls infra/admin/evaluator/Dockerfile infra/admin/evaluator/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile uses multi-stage or slim build for efficiency.
    - main.py logs successful initialization of Langfuse client.
  </acceptance_criteria>
  <done>Evaluator container code and config ready for deployment</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Evaluator into Admin Stack</name>
  <files>infra/admin/docker-compose.admin.yml</files>
  <action>
    1. Add `evaluator` service to `infra/admin/docker-compose.admin.yml`.
    2. Set environment variables: `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST` (pointing to `http://langfuse:3000`), `BIFROST_URL` (pointing to `http://bifrost:8080/v1`).
    3. Ensure it shares the `argus-network`.
    4. Build and start the container.
  </action>
  <verify>
    <automated>docker compose -f infra/admin/docker-compose.admin.yml up -d evaluator && docker compose -f infra/admin/docker-compose.admin.yml logs evaluator</automated>
  </verify>
  <acceptance_criteria>
    - Evaluator service is healthy and running in docker ps.
    - Logs show no connection errors to Langfuse or Bifrost.
  </acceptance_criteria>
  <done>Evaluator service fully integrated into the admin observability stack</done>
</task>

</tasks>

<verification>
Check that all services in the admin stack are up and that the evaluator logs show "Ready" status with successful connection to Langfuse.
</verification>

<success_criteria>
- Admin Bifrost routes to Ollama.
- Evaluator container is running and connected.
- No SaaS dependencies used for the evaluation infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-01-SUMMARY.md`
</output>
