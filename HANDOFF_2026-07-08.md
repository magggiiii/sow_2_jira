# Handoff — WAVE 7 shipped + hosted-SaaS pivot planned → build the offline deploy-ready layer

**Generated:** 2026-07-08
**Branch:** `elevation/wave-0` (HEAD: `914a367`, 11 commits ahead of last-pushed `9929ad3`)
**Checkpoint commit:** `914a367` (the docs commit that landed this handoff; local-only until pushed)
**Linear:** none — elevation tracked via `.planning/elevation/` + `UPGRADES.html` + project memory.
**Status:** WAVE 7 (offline completion) DONE + committed + verified green. UI/UX React revamp + dual-env deployment architecture PLANNED and LOCKED. Nothing pushed. Next = build the offline deploy-ready code (no infra needed).

## 1. What's done (verified this session)
| Item | Status |
|---|---|
| WAVE 7 R1 (5 gates: deps/deploy/tests/schema/orch+infr-15c) | DONE, committed `eef1d12`..`69384c3` |
| WAVE 7 R2+R3 (RunConfig.max_run_cost + repo-wide ruff clean) | DONE, `9aeab81` |
| beUI motion loaders (scramble/helix/newton, `motion` engine) | DONE, `bcaf1cc` (browser-verified animating) |
| Full backend suite | **881 pass / 0 fail / 2 skip** (was 831) |
| Frontend suite | vitest **111 pass** |
| Lint | `ruff check .` **clean** (CI lint now green); `make verify` EXIT 0 |
| LOCKED untouched | llm_router / observability shims / `llm_client.py:442` success_callback / bifrost / .keyfile — all intact |
| UI/UX revamp plan (React 19 migration) | committed `1e75063`; direction **Drafting Table** CONFIRMED `b402a23`. Plan: `.planning/ui-rework/UI-REWORK-PLAN.md` |
| Dual-env config (`APP_ENV` flip) | LOCKED `6f326e8`. Spec: `.planning/elevation/ENV-CONFIG.md`. Supabase for BOTH; MinIO dropped (local S3 endpoint verified from Supabase CLI source) |

## 2. Branch state (newest first)
```
914a367 docs: session checkpoint 2026-07-08 — WAVE 7 shipped + hosted-SaaS pivot planned/locked (this handoff)
6f326e8 docs(env): lock dual-environment APP_ENV flip config (Supabase both, MinIO dropped, verified local S3)
b402a23 docs(ui-rework): confirm Drafting Table design direction (decision log)
1e75063 docs(ui-rework): React migration + full UI/UX revamp plan (ultracode)
9aeab81 chore(wave7): RunConfig.max_run_cost (R2) + repo-wide ruff clean, CI lint green (R3)
bcaf1cc feat(ui): beUI motion loaders (scramble/helix/newton) via the motion engine
69384c3 feat(orchestrator): scoped dedup_against + CostMeter kill-switch + infr-15c de-OTel cleanup
67e3508 refactor(schemas): SourceRef raising validators, intelligence_schemas module, GoldenTicket retype
7e4dbc0 test(audit): AuditLogger 9-col schema coverage
9e58f0d feat(deploy): 4-service render.yaml + telemetry allowlist w/ cost_usd
eef1d12 build(deps): uv-pinned+hashed lockfile, PyPDF2->pypdf
```
All 10 above are UNPUSHED. Last pushed = `9929ad3` (both remotes).

## 3. The next session's mission — build the offline deploy-ready layer (no infra needed)
Do these in order; all buildable offline (no live services). Per-track TDD → adversarial-review workflow → explicit-path commit is the proven cadence.
1. **Config layer** per `.planning/elevation/ENV-CONFIG.md`: `APP_ENV=local|production` → `config/settings.py` (pydantic-settings) → `app/container.build_container()` selects adapters per `core/ports.py`. Author `.env.local` + a local bring-up (`supabase start` + Docker `redis:7`; `supabase` CLI not yet installed — `brew install supabase/tap/supabase`).
2. **Wave-2 offline code half (the big one — makes it deploy-ready):** STEP 2.1 `resolve_provider_config` (replaces the LOCKED `configure_litellm_for_mode` — executing Wave 2 is the intended time to unfreeze); 2.2 lifespan + idempotent init; 2.3 `pipeline/worker.py` + arq WorkerSettings + enqueue seam; 2.4 `JiraCredentials` model (env-creds, shared account — per-user deferred); 2.5 DB SessionStore + Google-OIDC route bodies; 2.6 un-gate current_user/ownership-404/upload hardening; pin `arq`/`redis`/`authlib`/`itsdangerous`/`boto3`/`pgvector`. All unit-testable on the sqlite+aiosqlite ORM shim.
3. **Wave-1 audit→SQLAlchemy:** rewrite `audit/logger.py` sqlite3 → SQLAlchemy writing `pipeline.db.AuditLog` (STEP 1.6d / infr-8). Test with the existing `tests/test_audit_logger.py`.
4. **Wave-4 OTel cleanup (cosmetic):** delete the dead `opentelemetry` callback at `pipeline/llm_client.py:441-443` + no-op meter `.add()/.record()` at ~:552-560 + fix stale `telemetry.py` comment. (Was LOCKED in WAVE 7 — GATED on decision #5 below.)
5. Then the **React "Drafting Table" revamp Phase 0** (tooling + core-port proof; 37 pure tests green day-one) — see `UI-REWORK-PLAN.md §4`.

## 4. DECISIONS NEEDED FROM THE USER (walk through these first)
The user asked to be re-walked through these. Present each with the recommendation:
1. **Local `AUTH_MODE`** — `local` (seeded dev-login, lets you test multi-user tenancy without Google) vs `off` (single fixed dev user). Rec: `local`.
2. **Queue library = `arq`?** (asyncio-native, Redis-backed, matches the seam). Rec: yes.
3. **Flip `use_pipeline_runner=True` now** (equivalence test is green) or defer to the React rework? Rec: flip now.
4. **UI revamp timing** — build in parallel with backend / after it / generate Drafting-Table mockups first for visual approval? + accept the plan's default decisions (Tailwind v4 tokens, Fraunces/Inter/IBM Plex Mono, HashRouter, wrap-then-migrate loaders, defer dark mode)?
5. **Delete the dead `opentelemetry` callback** in `llm_client.py`? It was LOCKED in WAVE 7 but the audit confirms it's inert dead code. Rec: delete (hygiene). Gates mission step 4.
6. **Push authorization** — push the 10 unpushed commits to both remotes? (GitHub `magggiiii`; GitLab `git@calib.dev` needs your VPN up.) + run the deferred WAVE 7 `UPGRADES.html` bar bump + `graphify` finalization.

## 5. Things explicitly NOT in scope (locked)
- **Service-walled (needs live infra — the go-live gate, NOT this session):** Supabase hosted Postgres, Render account + Key Value/Redis, R2/Supabase-Storage prod, Google OAuth client, `APP_ENC_KEY` prod secret, live LLM key + 103-node PDF (cassette), live Jira creds. Full shopping list in the last session's chat + per-wave in `.planning/elevation/ENV-CONFIG.md`.
- **LOCKED code:** `llm_router.py`/`configure_litellm_for_mode` (EXCEPT the intended Wave-2 STEP-2.1 replacement), `BIFROST_*`/bifrost.admin, `data/.keyfile`, `OLLAMA_*`/STEP 5.1 (keep Ollama+Bifrost — decided), `observability.py` shim DEFINITIONS.
- **Human/out-of-band:** rotate `ARGUS_BACKBONE_TOKEN` (in pushed history).
- Per-user Jira/LLM credential injection (STEP 2.4) — deferred; env-creds/shared account for now.

## 6. Key files for the deep dive
- `.planning/elevation/ENV-CONFIG.md` — the dual-env config spec (authoritative for step 1-2).
- `.planning/ui-rework/UI-REWORK-PLAN.md` — React migration + Drafting Table design (§4 phasing, §5 design, §8 decisions).
- `.planning/elevation/IMPLEMENTATION-PLAN.md` — STEP 2.x (auth/worker/creds), STEP 1.6d (audit→PG).
- `core/ports.py` + `app/container.py` (build_container) — the adapter seam the config layer plugs into.
- `pipeline/db.py` (7-table ORM + AuditLog + SQLite @compiles shims) · `audit/logger.py` (still sqlite3) · `config/settings.py` + `config/crypto.py`.
- `pipeline/jobs.py` + `core/queue_port.py` + `integrations/queue/*` (worker seam; `pipeline/worker.py` net-new).

## 7. Test data + how to run
- Backend: `PYTHONDONTWRITEBYTECODE=1 venv/bin/pytest tests/ -q --junit-xml=/tmp/x.xml` + parse XML (rtk eats the summary). Clear `__pycache__` first. Baseline **881/0/2**.
- Frontend: `cd ui && npm test` (111). Lint: `uvx ruff check .` (clean; ruff not in venv — use uvx). Import smoke: `make verify`.
- Local creds live in gitignored `.env` (Bifrost + Langfuse + Jira). Do not commit.

## 8. Documents to read in order
1. This handoff. 2. `.planning/elevation/ENV-CONFIG.md`. 3. `.planning/ui-rework/UI-REWORK-PLAN.md`. 4. Memory: `env-dual-setup.md`, `ui-rework-plan.md`, `elevation-progress.md`.

## 9. How to verify "done" at the end of next session
- Config layer: `APP_ENV=local` boots against the Supabase local stack; `build_container` returns local adapters; `.env.local` documented.
- Wave-2 offline: suite green (881 + new); `worker.py` + `resolve_provider_config` + `JiraCredentials` + DB SessionStore + OIDC routes exist and unit-test on sqlite shim; deps pinned/hashed; `ruff check .` clean.
- Wave-1 audit→SQLAlchemy green on `tests/test_audit_logger.py`; Wave-4 OTel cleanup grep-clean (if approved).
- Nothing pushed without explicit go-ahead.

## Still open (human)
- Rotate `ARGUS_BACKBONE_TOKEN`. · Push (both remotes; GitLab VPN). · Install `supabase` CLI for local dev.
