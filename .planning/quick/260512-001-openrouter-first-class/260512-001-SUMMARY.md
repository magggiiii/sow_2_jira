---
quick_id: 260512-001
slug: openrouter-first-class
date: 2026-05-12
status: complete
verification: smoke-script-pending (requires user's OPENROUTER_API_KEY)
---

# Quick Task 260512-001 — Summary

## Goal
First-class OpenRouter provider support. Backend plumbing was already present (`PROVIDER_REGISTRY`, `build_litellm_model`, `/api/providers/openrouter/models` discovery). Goal was to close UX and attribution gaps.

## Changes

| # | File | Commit | Change |
|---|------|--------|--------|
| 1 | `pipeline/llm_client.py` | `059c337` | Add `HTTP-Referer` + `X-Title` headers when provider is openrouter |
| 2 | `.env.example` | `f479112` | Document `OPENROUTER_API_KEY`, `OPENROUTER_REFERER`, `OPENROUTER_APP_NAME` |
| 3 | `README.md` | `8946674` | Add "Using OpenRouter" subsection under Launch and Configure |
| 4 | `ui/index.html`, `ui/app.js` | `238e37a` | Hidden helper line under model select; revealed when openrouter selected |
| 5 | `scripts/verify-openrouter.sh` | `3ed1ae7` | Bash smoke script: POST `/api/providers/openrouter/models`, validate JSON, print model count |

## Verification

- Code paths grep-confirmed:
  - `grep "OpenRouter attribution" pipeline/llm_client.py` → present
  - `grep "OPENROUTER_API_KEY" .env.example` → present
  - `grep "Using OpenRouter" README.md` → present
  - `grep "openrouterModelHelp" ui/index.html ui/app.js` → 4 matches, both files
  - `scripts/verify-openrouter.sh` → executable (`-rwxr-xr-x`)
- **End-to-end smoke test pending**: the user must run `OPENROUTER_API_KEY=sk-or-... bash scripts/verify-openrouter.sh` against a live local server. This is intentional — we don't have their key in the repo and OpenRouter usage is billed.

## What this does NOT change

- The 300-model `<select>` picker stays as-is (per user instruction — "we already have a list models"). Typeahead/combobox is a separate UX task if needed.
- No default OpenRouter model is hard-coded; users pick after Fetch Models.
- No changes to provider routing logic in `pipeline/llm_router.py` — discovery and config already covered openrouter.

## Operator follow-up

1. Boot the stack: `make ui` (local) or `s2j` (Docker).
2. Open http://localhost:8000 → Settings → select `openrouter` → paste `sk-or-...` → Fetch Models.
3. Or, run from terminal: `OPENROUTER_API_KEY=sk-or-... bash scripts/verify-openrouter.sh`

## Known issue (out of scope)

`node` is broken on the dev machine (missing `libllhttp.9.3.dylib` after homebrew upgraded `llhttp` to 9.4). This prevented `gsd-tools.cjs` from running; the quick task was executed manually with identical discipline (atomic commits, `.planning/quick/` artifacts, STATE.md update pending). Fix: `brew reinstall node` when convenient.
