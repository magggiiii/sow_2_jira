---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - scripts/seed_full_langfuse_dataset.py
  - infra/admin/evaluator/main.py
  - tests/test_phase11_evals.py
autonomous: true
requirements: [EVAL-02, EVAL-03]

must_haves:
  truths:
    - "Langfuse dataset contains exhaustive ground-truth tickets for all features"
    - "Evaluations for Faithfulness, Comprehensiveness, and Format Adherence run successfully and appear in Langfuse UI"
    - "Evaluation scores are successfully pushed back to Langfuse"
  artifacts:
    - path: "infra/admin/evaluator/main.py"
      provides: "Langchain evaluation and Langfuse scoring logic"
  key_links:
    - from: "s2j-admin-evaluator"
      to: "s2j-admin-bifrost"
      via: "http://bifrost:8080/v1"
    - from: "s2j-admin-evaluator"
      to: "s2j-admin-langfuse"
      via: "http://langfuse:3000"
---

<objective>
Implement the evaluation logic using Langfuse Python SDK and LangChain.

Purpose: To automatically score production traces for precision/recall and format adherence.
Output: Seeded Langfuse dataset and Python evaluation script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-evals-architecture/11-CONTEXT.md
@.planning/phases/11-evals-architecture/11-RESEARCH.md
@scripts/seed_full_langfuse_dataset.py
@infra/admin/evaluator/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed Ground-Truth Dataset in Langfuse</name>
  <files>scripts/seed_full_langfuse_dataset.py</files>
  <action>
Execute the seed script to ensure the dataset exists before the evaluator attempts to fetch it.
Run `python scripts/seed_full_langfuse_dataset.py` to seed the 'sow-extraction-full-ground-truth' dataset.
  </action>
  <verify>
    <automated>python -c "import os; os.environ['LANGFUSE_HOST'] = os.getenv('LANGFUSE_HOST', 'http://localhost:3002'); from langfuse import Langfuse; assert Langfuse().get_dataset('sow-extraction-full-ground-truth') is not None, 'Dataset not found'"</automated>
  </verify>
  <done>Langfuse dataset contains ground-truth tickets for all features</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Langchain Evaluator Script</name>
  <files>infra/admin/evaluator/main.py, tests/test_phase11_evals.py</files>
  <read_first>scripts/seed_full_langfuse_dataset.py, infra/admin/evaluator/main.py</read_first>
  <action>
Create the main script that pulls traces and scores them.

1. Create `infra/admin/evaluator/main.py`.
2. Initialize `os.environ` defaults for `LANGFUSE_HOST`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY` matching the compose file.
3. Initialize `langfuse = Langfuse()`.
4. Initialize `llm = ChatOpenAI(openai_api_base=os.getenv("OPENAI_API_BASE"), openai_api_key=os.getenv("OPENAI_API_KEY"), model_name="gpt-4")` from `langchain_openai`.
5. Create `def run_evaluations():`
   - Use `langfuse.get_dataset("sow-extraction-full-ground-truth")` to get dataset.
   - For items in dataset, simulate or fetch evaluation logic.
   - Call `langfuse.score(trace_id=item.trace_id, name="Faithfulness", value=score)`
   - Call `langfuse.score(trace_id=item.trace_id, name="Comprehensiveness", value=score)`
   - Call `langfuse.score(trace_id=item.trace_id, name="Format Adherence", value=score)`
6. Add scheduling: `schedule.every(int(os.getenv("EVAL_INTERVAL_MINUTES", "60"))).minutes.do(run_evaluations)`
7. Add a loop to run `schedule.run_pending()` continuously.
8. Update `tests/test_phase11_evals.py` to mock `ChatOpenAI` and `Langfuse` to verify that `run_evaluations()` executes without errors.
  </action>
  <verify>
    <automated>pytest tests/test_phase11_evals.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def run_evaluations" infra/admin/evaluator/main.py` returns 0.
    - `grep "langfuse.score" infra/admin/evaluator/main.py` returns 0.
    - `grep "ChatOpenAI" infra/admin/evaluator/main.py` returns 0.
    - `grep "schedule.every" infra/admin/evaluator/main.py` returns 0.
  </acceptance_criteria>
  <done>Evaluator logic is complete and scheduled, and tests pass</done>
</task>

</tasks>

<verification>
- Verify the main.py script can be executed and tests pass
</verification>

<success_criteria>
- Evaluations for Faithfulness, Comprehensiveness, and Format Adherence are defined
- The execution relies on Langchain and records to Langfuse
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-02-SUMMARY.md`
</output>