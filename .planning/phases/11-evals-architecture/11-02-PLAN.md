---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - infra/admin/evaluator/main.py
  - tests/test_phase11_evals.py
autonomous: true
requirements: [EVAL-02, EVAL-03]

must_haves:
  truths:
    - "Evaluations for Faithfulness, Comprehensiveness, and Format Adherence run successfully and appear in Langfuse UI"
    - "Evaluation scores are successfully pushed back to Langfuse"
    - "Evaluator logic uses the isolated admin Bifrost proxy for LLM calls"
  artifacts:
    - path: "infra/admin/evaluator/main.py"
      provides: "Langchain evaluation and Langfuse scoring logic"
  key_links:
    - from: "s2j-admin-evaluator"
      to: "s2j-admin-bifrost"
      via: "http://bifrost:8080/v1"
    - from: "s2j-admin-evaluator"
      to: "s2j-admin-langfuse"
      via: "http://langfuse:3000"
---

<objective>
Implement the core evaluation logic using Langfuse Python SDK and LangChain evaluation chains.

Purpose: To automatically score production traces for precision, recall, and adherence to established formats using LLM-as-a-judge.
Output: Fully functional evaluation script running within the background service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-evals-architecture/11-CONTEXT.md
@.planning/phases/11-evals-architecture/11-RESEARCH.md
@scripts/seed_full_langfuse_dataset.py
@infra/admin/evaluator/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Langchain Evaluator Logic</name>
  <files>infra/admin/evaluator/main.py, tests/test_phase11_evals.py</files>
  <read_first>infra/admin/evaluator/main.py</read_first>
  <action>
Implement the evaluation script that fetches dataset items and scores them.

1. Update `infra/admin/evaluator/main.py`:
   - Initialize `Langfuse` client.
   - Initialize `ChatOpenAI` (from `langchain_openai`) using admin Bifrost environment variables.
   - Implement `run_evaluations()`:
     - Fetch `sow-extraction-full-ground-truth` dataset.
     - For each item, run Langchain evaluation chains (Faithfulness, Comprehensiveness).
     - Submit scores back to Langfuse using `langfuse.score()`.
   - Implement the scheduling loop using the `schedule` library.

2. Update `tests/test_phase11_evals.py`:
   - Mock `Langfuse` and `ChatOpenAI` to verify the evaluation flow.
   - Assert that `langfuse.score` is called with correct parameters.
  </action>
  <verify>
    <automated>docker compose -f infra/admin/docker-compose.admin.yml exec evaluator pytest tests/test_phase11_evals.py</automated>
  </verify>
  <done>Evaluator logic is implemented, passing tests inside the container, and running on schedule</done>
</task>

</tasks>

<verification>
- `docker compose logs evaluator` shows successful evaluation runs.
- Langfuse UI displays new scores for traces.
</verification>

<success_criteria>
- Faithfulness, Comprehensiveness, and Format Adherence metrics are recorded.
- The background service executes without errors or impact on production traffic.
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-02-SUMMARY.md`
</output>
