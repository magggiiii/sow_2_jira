---
phase: 11-evals-architecture
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [
  infra/admin/evaluator/runner.py,
  infra/admin/evaluator/Dockerfile,
  infra/admin/evaluator/requirements.txt,
  infra/admin/docker-compose.admin.yml
]
autonomous: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "Evaluation scores are successfully pushed to Langfuse"
    - "Evaluator service runs independently in the admin stack"
    - "Evaluator does not use the production user proxy (s2j-user-bifrost)"
  artifacts:
    - path: "infra/admin/evaluator/runner.py"
      provides: "Batch dataset evaluation execution"
    - path: "infra/admin/evaluator/Dockerfile"
      provides: "Evaluator container environment"
  key_links:
    - from: "infra/admin/evaluator/runner.py"
      to: "pipeline/agents/extraction.py"
      via: "import and execution"
---

<objective>
Deploy the evaluation logic as a service in the admin stack and implement the batch runner that executes the extraction engine against the Golden Dataset, scoring the results and pushing them back to Langfuse.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-evals-architecture/11-01-PLAN.md
@infra/admin/docker-compose.admin.yml
@pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dataset Test Runner</name>
  <files>infra/admin/evaluator/runner.py</files>
  <read_first>
    - infra/admin/evaluator/logic.py (for HierarchicalEvaluator usage)
    - pipeline/orchestrator.py (for extraction logic reference)
  </read_first>
  <action>
    Create `infra/admin/evaluator/runner.py` that automates the evaluation loop:
    1. Initialize Langfuse client and fetch items from `sow-hierarchical-ground-truth`.
    2. For each item:
       - Instantiate `TaskExtractionAgent` with appropriate configuration.
       - Run `extract()` on the item's `section_text`.
       - Pass results to `HierarchicalEvaluator.evaluate()`.
       - Create a "Dataset Run" in Langfuse.
       - Use `langfuse.score()` to attach numerical scores for Alignment, Recall, and Hallucination to each trace.
    3. Ensure proper error handling and logging for the batch process.
  </action>
  <verify>
    <automated>python3 infra/admin/evaluator/runner.py --dry-run || [ $? -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `runner.py` correctly integrates `TaskExtractionAgent` and `HierarchicalEvaluator`.
    - Script supports a mode to execute against the dataset and push to Langfuse.
  </acceptance_criteria>
  <done>Dataset test runner is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build and Integrate Evaluator Container</name>
  <files>infra/admin/evaluator/Dockerfile, infra/admin/evaluator/requirements.txt, infra/admin/docker-compose.admin.yml</files>
  <read_first>
    - infra/admin/docker-compose.admin.yml (to add new service)
  </read_first>
  <action>
    1. Create `infra/admin/evaluator/requirements.txt` with: `langfuse`, `langchain`, `langchain-openai`, `tenacity`.
    2. Create `infra/admin/evaluator/Dockerfile` based on `python:3.11-slim`.
       - Mount the root project directory to ensure access to `pipeline/` and `models/`.
       - Install dependencies.
    3. Add `evaluator` service to `infra/admin/docker-compose.admin.yml`:
       - `image: s2j-evaluator:latest` (or build locally).
       - Environment: `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST=http://langfuse:3000`, `BIFROST_URL=http://bifrost:8080/v1`.
       - `depends_on`: `langfuse`, `bifrost`.
       - `networks`: `argus-network`.
       - `volumes`: Mount root dir to `/app`.
  </action>
  <verify>
    <automated>docker compose -f infra/admin/docker-compose.admin.yml config | grep -q "evaluator"</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` and `requirements.txt` exist.
    - `docker-compose.admin.yml` includes the `evaluator` service with correct network and environment settings.
  </acceptance_criteria>
  <done>Evaluator is containerized and integrated into the admin stack.</done>
</task>

</tasks>

<verification>
Start the admin stack and run a test evaluation pass via the container. Verify that scores appear in the Langfuse "Datasets" tab.
</verification>

<success_criteria>
1. Evaluator service correctly containerized and connected to Admin Bifrost and Langfuse.
2. Batch evaluation pass successfully populates scores in Langfuse without impacting user-facing services.
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-02-SUMMARY.md`
</output>
