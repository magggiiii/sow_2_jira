---
phase: 01-provider-consistency
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - models/schemas.py
  - pipeline/llm_router.py
  - pipeline/llm_client.py
  - pipeline/orchestrator.py
  - pageindex/utils.py
autonomous: true
requirements:
  - PROV-01
must_haves:
  truths:
    - "Per-run LLM configuration is isolated"
    - "No os.environ leaks for LLMs"
  artifacts:
    - path: "models/schemas.py"
      provides: "ProviderConfig model"
    - path: "pipeline/llm_client.py"
      provides: "LLMClient with ProviderConfig"
  key_links:
    - from: "models/schemas.py"
      to: "pipeline/llm_client.py"
      via: "RunConfig.provider_config"
      pattern: "self.provider_config"
---

<objective>
Enforce per-run immutable LLM config snapshot in orchestration path.

Purpose: Prevent concurrent extraction runs from interfering with each other via global `os.environ` mutations.
Output: Codebase uses explicit kwargs and contextvars instead of mutating environment variables for credentials.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-provider-consistency/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ProviderConfig to schemas</name>
  <read_first>
    - models/schemas.py
  </read_first>
  <files>
    - models/schemas.py
  </files>
  <action>
    In `models/schemas.py`, add:
    ```python
    class ProviderConfig(BaseModel):
        provider: str
        model: str
        api_key: str = ""
        api_base: str = ""
        azure_api_version: str = ""
        azure_deployment_name: str = ""
    ```
    Update `class RunConfig(BaseModel):` to add `provider_config: Optional[ProviderConfig] = None`.
    Also add `import contextvars` and `current_provider_config = contextvars.ContextVar("current_provider_config", default=None)` near the top of the file so other modules can use it for thread-safe implicit injection.
  </action>
  <acceptance_criteria>
    - `models/schemas.py contains class ProviderConfig(BaseModel):`
    - `models/schemas.py contains provider_config: Optional[ProviderConfig] = None`
    - `models/schemas.py contains ContextVar("current_provider_config"`
  </acceptance_criteria>
  <done>ProviderConfig model is defined and added to RunConfig.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor llm_router.py</name>
  <read_first>
    - pipeline/llm_router.py
  </read_first>
  <files>
    - pipeline/llm_router.py
  </files>
  <action>
    In `pipeline/llm_router.py`, remove ALL `os.environ` mutations (e.g. `os.environ["LITELLM_API_KEY"] = ...`).
    Remove the duplicated Fernet logic (`_derive_fernet_key`, `_load_fernet`, `_decrypt_secret`).
    Remove `_load_settings`, `_apply_provider_settings`, `_build_litellm_model`.
    Import `SettingsManager` and `build_litellm_model` from `config.settings`.
    Refactor `configure_litellm_for_mode(mode: LLMMode) -> "ProviderConfig":` to instantiate `SettingsManager`, load settings, decrypt credentials, and return a `ProviderConfig` instance instead of returning a string.
    Ensure fallback to Bifrost env vars (like `BIFROST_API_KEY`, `ZAI_MODEL`, `OLLAMA_BASE_URL`) remains intact for API/LOCAL modes, but assign them to the `ProviderConfig` object rather than mutating `os.environ`.
  </action>
  <acceptance_criteria>
    - `pipeline/llm_router.py contains configure_litellm_for_mode(mode: LLMMode) -> ProviderConfig`
    - `pipeline/llm_router.py does not contain os.environ["LITELLM_API_KEY"] = `
  </acceptance_criteria>
  <done>Router returns ProviderConfig without mutating global environment.</done>
</task>

<task type="auto">
  <name>Task 3: Inject Config into LLMClient and PageIndex</name>
  <read_first>
    - pipeline/llm_client.py
    - pipeline/orchestrator.py
    - pageindex/utils.py
  </read_first>
  <files>
    - pipeline/llm_client.py
    - pipeline/orchestrator.py
    - pageindex/utils.py
  </files>
  <action>
    In `pipeline/llm_client.py`, update `__init__` to accept `provider_config: ProviderConfig = None`. Set `self.provider_config = provider_config or configure_litellm_for_mode(mode)`. Set `self.model = self.provider_config.model`.
    Update `complete()`: remove `custom_api_key = os.environ.get(...)` and inject directly into `kwargs`: `kwargs["api_key"] = self.provider_config.api_key`, `kwargs["api_base"] = self.provider_config.api_base`, `kwargs["model"] = self.provider_config.model`. Only add if they are truthy.
    
    In `pipeline/orchestrator.py`'s `run()` method, right before building the indexer, call `configure_litellm_for_mode(config.llm_mode)` if `config.provider_config` is None, and assign it to `config.provider_config`. Set `current_provider_config.set(config.provider_config)` using the ContextVar from `models/schemas.py`. Pass `provider_config=config.provider_config` to `LLMClient(...)`. `pageindex_model` should be extracted from `config.provider_config.model`.
    
    In `pageindex/utils.py`, import `current_provider_config` from `models.schemas`. Update `llm_completion` and `llm_acompletion`. If `current_provider_config.get()` returns a valid config, extract its `api_key` and `api_base` and inject them into the `litellm.completion` or `litellm.acompletion` kwargs. This prevents PageIndex from relying on `os.environ`.
  </action>
  <acceptance_criteria>
    - `pipeline/llm_client.py contains self.provider_config = provider_config`
    - `pipeline/orchestrator.py contains current_provider_config.set`
    - `pageindex/utils.py contains current_provider_config.get()`
  </acceptance_criteria>
  <done>LLM client and PageIndex receive credentials directly.</done>
</task>

</tasks>

<verification>
No mutations to global OS environment variables.
</verification>

<success_criteria>
- Concurrent runs can use different credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-consistency/01-02-SUMMARY.md`
</output>
