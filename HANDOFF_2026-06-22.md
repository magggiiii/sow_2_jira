# Handoff — guardrail band complete (C-4 corpus filter + PushGate built & wired) → STEP 3.6 / 5.4

**Generated:** 2026-06-22 (continues `HANDOFF_2026-06-15_PART_4.md`)
**Branch:** `elevation/wave-0` (HEAD = the checkpoint commit below, ~69 ahead of `main`; run `git log -1` for the exact SHA)
**Checkpoint commit:** the most recent `docs: session checkpoint 2026-06-22 …` commit — run `git log -1` (amend rewrites the hash, so it isn't embedded here)
**Linear:** [SOW-to-Jira Elevation](https://linear.app/calibraint-ai/project/sow-to-jira-elevation-883972f170ac) (team CAL). **DEFERRED — no comment posted (locked).**
**Status:** **Guardrail band fully shipped — Coverage gate + embedding-tier corpus filter + PushGate (built AND wired into the live push path).** Suite **470 passed / 1 skipped, EXIT 0**. `main` untouched, **nothing pushed**.

## 1. What's done (this session, all TDD + adversarial-review workflows, suite green before each commit)

| Increment | Commit | Status |
|-----------|--------|--------|
| **C-4 / STEP 3.5 — embedding-tier coverage corpus filter** (`pipeline/features/coverage_filter.py`): tiers each reported miss vs the final deduped corpus (drop ≥0.85 already-covered, advisory 0.70–0.85, genuine <0.70); only genuine misses survive to `CoverageGate`. Env-gated `SOW_COVERAGE_CORPUS_FILTER` (default OFF for eval determinism; ON in prod). Reuses dedup MiniLM via an injectable `embed_fn`. | `8d5eee5` | ✅ 13 tests; review 0 defects |
| **STEP 3.5 — PushGate** (`core/guardrails.py`): pure, read-only push-admission gate. Skip-first (`jira_issue_key` → idempotent skip), block INCOMPLETE/LOW_CONFIDENCE/AMBIGUOUS_SCOPE/NO_ACCEPTANCE_CRITERIA + any DEGRADED-run task unless `override`. `assert_pushable → PushGateResult`, raises `PushBlocked`. Fail-closed on scalar flags. | `2da7024` | ✅ 23 tests; review 0 defects |
| **STEP 5.3 — wire PushGate live + delete dead MCP client**: `ui/server._gate_push` runs approved tasks through PushGate before the Jira create loop (never raises — blocked→failed result, already-pushed→skip, only cleared→pushed); `PushRequest.override`; deleted `integrations/jira_mcp_client.py` + `scripts/check_jira_mcp.py` (unwired). | `4b9e8a7` | ✅ 7 tests; review 0 defects |
| Suite | `venv/bin/pytest tests/ -q -p no:warnings` → **470 passed / 1 skipped, EXIT 0** | ✅ |
| graphify | refreshed via CLI `graphify update .` after each increment | ✅ |

**Guardrail band is now COMPLETE:** CoverageGate (structural C-4) + corpus filter (the rate-drop mechanism) + the critic's `ConfidenceGate` consolidation (`pipeline/agents/critic.py:200,291` — so a separate `CriticGate` class would be redundant dead code; STEP 3.5 is substantively done) + PushGate (built AND wired). The loop closes: coverage/critic flags a task → push refuses it.

## 2. Branch state (newest at top; since PART_4 checkpoint `76fcf06`)
```
(HEAD) docs: session checkpoint 2026-06-22 — this commit (run `git log -1` for SHA)
a15627a docs(elevation): tracker — PushGate wired into live push path (STEP 5.3)
4b9e8a7 feat(push): wire PushGate into the live push path + delete unwired MCP client (STEP 5.3)
5a35869 docs(elevation): tracker — PushGate (STEP 3.5) shipped
2da7024 feat(guardrails): PushGate — deterministic Jira push admission gate (STEP 3.5)
5ea3581 docs(elevation): tracker — C-4 embedding-tier corpus filter shipped
8d5eee5 feat(coverage): embedding-tier corpus filter — drop cross-section dup misses (C-4 / STEP 3.5)
76fcf06 docs: session checkpoint 2026-06-15 (PART 4)  ← prior handoff
```

## 3. The next session's mission — offline-verifiable backlog, in order

The full roadmap was mapped this session (3 Explore agents). **Offline-verifiable, non-locked remaining work, recommended order:**

1. **STEP 5.4 — move tuning knobs into `RunConfig`** (small, clean, self-contained). Today these are read via `os.getenv` in `pipeline/orchestrator.py:__init__` (~lines 92–127): `EXTRACTION_CONFIDENCE_THRESHOLD` (0.6), `DEDUP_SIMILARITY_THRESHOLD` (0.85), `SOW_CLASSIFIER_ENABLED`/`SOW_ENABLE_CRITIC`/`SOW_SEMANTIC_COVERAGE` (1), `SOW_CRITIC_THRESHOLD` (0.8), `max_section_chars` (16000), `max_gap_recovery_iterations`, `SOW_COVERAGE_MIN_CONFIDENCE`, `SOW_COVERAGE_CORPUS_FILTER`, `SOW_NODE_CONCURRENCY`. Add typed fields to `RunConfig` (`models/schemas.py`) with the SAME defaults; read from config with env as fallback (back-compat). TDD; assert defaults unchanged. Verify: suite green.
2. **STEP 3.6 — prompts registry + per-agent AgentSpec adoption** (bigger; foundational for the 3.7 flip). `core/agent_runner.py` + `core/agent_spec.py` already shipped. Pending: create `prompts/registry.py` + `prompts/*.v1.txt` (BYTE-FAITHFUL move of the inline f-strings — use snapshot tests), and refactor the 6 agents (extraction, classifier, dedup, critic, coverage, gap_recovery) to build an `AgentSpec` + call the runner, proving identical mutations. Fully offline.
3. **STEP 3.7 — extract stages + PEV-order `PipelineRunner` behind a flag** with an OFFLINE equivalence golden test (run the existing synthetic eval-harness cassette through both `orchestrator.run()` and the runner; assert identical task_count/merge/flag set). Land behind a flag; **do NOT delete `run()`** until the faithful 103-node cassette (3.8) lands. `core/pipeline/runner.py` + registry already exist from W0.

**Per-increment cadence (established this session):** scout/design workflow (when there are real forks) → TDD in the main loop (RED→GREEN, run the suite yourself) → adversarial-review workflow (dimensions → adversarial verify) → commit → update `.planning/elevation/UPGRADES.html` → `graphify update .`.

## 4. Open behavioral risks to look for
- **Corpus filter is default-OFF.** It only drops the real-run INCOMPLETE rate when `SOW_COVERAGE_CORPUS_FILTER=1` (prod env). The INV-4 harness keeps it OFF for determinism; the faithful proof is the recorded cassette (3.8). Don't mistake green INV-4 for "filter validated end-to-end."
- **PushGate run_status = DEGRADED only** (`is_degraded`), NOT FAILED — a FAILED run would not be blocked. Documented follow-up if a FAILED run can reach push.
- **STEP 5.4 RunConfig change is a contract change** — keep env-fallback + identical defaults so existing runs/checkpoints don't drift; `RunConfig` is persisted in `pipeline_output.json` config block.
- **STEP 3.6 byte-faithfulness** — moving inline prompts to `.txt` must preserve exact wording (snapshot test) or extraction behavior shifts.
- **Stale docs:** `jira_mcp_client` is deleted but still referenced in CLAUDE.md/AGENTS.md **GSD-managed blocks** — refresh via `/gsd:map-codebase` (regenerate, don't hand-edit).

## 5. Things explicitly NOT in scope (locked)
- **Service-blocked (need provisioning + go-ahead):** Waves 1 (Postgres/secrets), 2 (Redis worker/OAuth), 4.2 (Render), STEP 3.9 (pgvector). Code is designed but can't be production-verified offline.
- **Live-run-blocked:** STEP 3.8 faithful 103-node cassette (real gemini on a real SOW + dedup-UUID remap — see memory `inv4-cassette-dedup-uuid`); the STEP 3.7 *final* `run()` deletion is gated on it.
- **STEP 5.1 (remove `LLMMode.LOCAL`/Ollama/Bifrost) — LOCKED.** Do NOT remove them.
- Do NOT touch `pipeline/llm_router.py` / `BIFROST_*` / `os.environ` credential paths. No push/PR/deploy without go-ahead. No Linear comment (deferred). gemini-2.5-flash + `Mode.JSON` is the validated structured-output default.

## 6. Key files for the deep dive
- `core/guardrails.py` — `ConfidenceGate`, `CoverageGate`, `PushGate` (the gate family + the pattern to mirror).
- `pipeline/features/coverage_filter.py` — `CoverageCorpusFilter` (feature-stage; numpy OK).
- `pipeline/orchestrator.py` — `__init__` env knobs (STEP 5.4 target), `_run_coverage_verify` / `_coverage_embed_fn`, the health/run() assembly.
- `ui/server.py` — `_gate_push` + `run_push_task` (the wired push path).
- `core/agent_runner.py` + `core/agent_spec.py` — STEP 3.6 substrate (already shipped).
- `pipeline/evals/{harness,cassette,replay,recorder,bands}.py` — INV-4 gate + the recorder seam.
- `.planning/elevation/IMPLEMENTATION-PLAN.md` (STEP 5.4 ≈ orch-11/doma-10; 3.6 ≈ STEP 3.6; 3.7), `UPGRADES.html` (living tracker), `AUDIT.md`.

## 7. Test data + how to verify
- Suite: `venv/bin/pytest tests/ -q -p no:warnings` → 470 passed / 1 skipped (offline; all LLM boundaries stubbed/replayed). ~65s.
- Guardrail tests: `tests/test_coverage_gate.py`, `tests/test_coverage_corpus_filter.py`, `tests/test_push_gate.py`, `tests/test_push_path_gating.py`, `tests/test_eval_harness.py` (INV-4 controls).
- Offline structured-output wiring: `venv/bin/python scripts/check_structured_output.py --self-test` → 6/6.
- venv: `venv/bin/python` (3.11.15); `instructor`, `litellm`, `pydantic` v2, `sentence-transformers` (MiniLM).

## 8. Documents to read in order
1. This handoff.
2. `HANDOFF_2026-06-15_PART_4.md` (C-4 restructure + INV-4 recorder + the dedup-UUID blocker).
3. `.planning/elevation/IMPLEMENTATION-PLAN.md` (STEP 3.6, 3.7, 5.4) + `UPGRADES.html` (living tracker) + `AUDIT.md`.
4. Memory: `elevation-render-saas-direction.md`, `keep-upgrades-tracker-updated.md`, `inv4-cassette-dedup-uuid.md`, `graphify-refresh-method.md`, `linear-guidebook.md`.

## 9. How to verify "done" next session
- [ ] STEP 5.4: tuning knobs in `RunConfig` (typed, env-fallback, identical defaults); suite green; no behavior drift.
- [ ] STEP 3.6: prompts registry + agents on `AgentSpec`; byte-faithful (snapshot tests); per-agent identical-mutation tests; suite green.
- [ ] Each increment: re-run suite yourself, adversarial-review workflow → 0 confirmed defects, commit, update `UPGRADES.html`, `graphify update .`.
- [ ] Suite ≥ 470 green; no `llm_router`/BIFROST/`os.environ`-cred file touched; no push/PR; Linear deferred; `LLMMode.LOCAL`/Ollama/Bifrost untouched.
