# Handoff — offline elevation backlog complete + branch shipped to remotes → resume elevation upgrades

**Generated:** 2026-07-04 (continues `HANDOFF_2026-06-22_PART_2.md`)
**Branch:** `elevation/wave-0` (HEAD = the checkpoint commit below; run `git log -1` for the exact SHA — amend rewrites the hash)
**Checkpoint commit:** the most recent `docs: session checkpoint 2026-07-04 …` commit — run `git log -1`.
**Linear:** [SOW-to-Jira Elevation](https://linear.app/calibraint-ai/project/sow-to-jira-elevation-883972f170ac) (team CAL). **DEFERRED — no comment posted (locked).**
**Status:** **Offline-verifiable backlog COMPLETE (5.4 + 3.6 + 3.7). Branch pushed to BOTH remotes; GitHub PR #7 open.** Suite **554 passed / 1 skipped, EXIT 0**. No new code this session — it was push/PR + roadmap review. Next: resume the remaining *offline-buildable* elevation upgrades and keep the live tracker HTML updated as you go.

## 1. What's done
| Item | Status |
|------|--------|
| STEP 5.4 (tuning knobs → RunConfig), 3.6a (prompts registry), 3.6b (AgentSpec adoption), 3.7 (staged PipelineRunner + equivalence golden) | ✅ shipped prior session (`1ed67aa`,`5f597d7`,`86f5f4c`,`7ff37d4`) |
| Suite | ✅ 554 passed / 1 skipped, EXIT 0 |
| Push to **GitLab** (`git@calib.dev:mageswaran/sow_2_jira`, SSH) | ✅ `elevation/wave-0` @ `4b6b8eb` |
| Push to **GitHub** (`magggiiii/sow_2_jira`) | ✅ `elevation/wave-0` @ `4b6b8eb` |
| **PR #7** (`elevation/wave-0` → `main`) | ✅ https://github.com/magggiiii/sow_2_jira/pull/7 |
| Tracker `UPGRADES.html` | ✅ dated 2026-07-04, push/PR noted, Wave-5 item corrected |

## 2. Branch state (newest first; since prior handoff `c98d25b`)
```
(HEAD) docs: session checkpoint 2026-07-04  ← this commit (run git log -1 for SHA)
4b6b8eb docs: mark STEP 3.7 shipped — offline-verifiable backlog complete
7ff37d4 feat(pipeline): staged PipelineRunner behind a flag + offline equivalence golden (STEP 3.7)
c98d25b docs: session checkpoint 2026-06-22 (PART 2) — STEP 5.4 + 3.6 shipped, 3.7 scoped
```
(Both remotes are at `4b6b8eb`; this checkpoint commit makes local 1 ahead of the pushed branch/PR — push it only with go-ahead, see §4.)

## 3. The next session's mission — resume elevation upgrades (offline-buildable), keep the tracker current

Work these IN ORDER, one increment at a time, full cadence each (TDD RED→GREEN → adversarial-review workflow → commit → **update `.planning/elevation/UPGRADES.html`** → `graphify update .`). **Keeping the living tracker current after every increment is a first-class deliverable (user directive).**

1. **Wave 5 — inverted Jira issue-link direction.** The Jira issue-link is created with the inward/outward direction reversed. Fix in `integrations/jira_client.py` (search `issue_link` / `create_issue_link` / `inward`/`outward`). TDD against the existing jira_client tests (mock the SDK). Verify: suite green + a new direction-assertion test.
2. **Wave 5 — typed exception classification.** Replace broad/ambiguous `except`/`RuntimeError` handling on the Jira-push + pipeline error boundaries with typed, classified exceptions (transient vs terminal vs user-fixable) so callers/UI can react. Scope from `IMPLEMENTATION-PLAN.md` Wave 5 change-ids. TDD.
3. **STEP 3.8 dedup-UUID remapper (offline sub-component).** Build + unit-test the **replay-time** remapper that rewrites a recorded `DeduplicationAgent` decision's `task_id_a/b` onto the current run's fresh UUIDs (match by title / source node_id / position). Lives on the REPLAY side — extend `make_structured_replay` (`pipeline/evals/replay.py`) or the harness, NOT the recorder (recorder seam already shipped). Test against SYNTHETIC recorded cassettes. This de-risks the live-run cassette (②) without needing a live run. See memory `inv4-cassette-dedup-uuid`.

**Then STOP** at the ②/③ walls (below) and confirm direction with the user before anything needing a live model run or provisioned services.

## 4. Open behavioral risks / gotchas
- **GitHub push account gotcha.** `gh` has TWO accounts; only the **active** one is served to git for github.com. `magggiiii` (repo owner) MUST be active or pushes 403 as `Maaagggi`. This session set active→`magggiiii` and set `origin` = `https://magggiiii@github.com/magggiiii/sow_2_jira.git`. If a push 403s: `gh auth switch --user magggiiii`.
- **GitLab is SSH** (`git@calib.dev:…`); the ed25519 key is registered on calib.dev. `ssh -T git@calib.dev` should greet `@mageswaran`.
- **Pushing new commits updates PR #7** — do so only with explicit go-ahead (locked constraint). Local checkpoint commits stay local.
- **macOS Documents permission.** If git/file reads return `EPERM: Operation not permitted`, the terminal/Claude process lost `~/Documents` access — grant **Full Disk Access** (System Settings → Privacy & Security) and restart the terminal.
- **Leaked GitHub PAT** (`ghp_…`, now dead + stripped from the remote) should still be **rotated**.
- **`rtk` eats pytest's summary line** — verify with `--junit-xml=/tmp/x.xml` + parse, or trust `EXIT=0` + JUnit counts. Don't trust dot counts.

## 5. Things explicitly NOT in scope (locked)
- **STEP 5.1** — removing `LLMMode.LOCAL`/Ollama/Bifrost. **LOCKED. Do not.**
- **Live-run-blocked:** STEP 3.8 *faithful 103-node recorded cassette* (real gemini on a real SOW) + the STEP 3.7 *final* `run()` deletion (gated on that cassette). The 3.8 *remapper* (§3.3) IS in scope offline.
- **Service-blocked:** Wave 1 (Postgres/R2/secrets), Wave 2 (Redis worker/OAuth), Wave 4 (Render/observability), STEP 3.9 (pgvector). Designed behind seams; can't be verified offline.
- Do NOT touch `pipeline/llm_router.py` / `BIFROST_*` / `os.environ` credential paths. No push/PR/deploy without go-ahead. No Linear comment (deferred). `gemini-2.5-flash` + `Mode.JSON` is the validated structured-output default.

## 6. Key files
- `integrations/jira_client.py` — Jira issue-link direction (§3.1) + push error boundaries (§3.2).
- `pipeline/evals/replay.py` (`make_structured_replay`, `NoOpProvider`), `pipeline/evals/harness.py` (`EvalHarness`), `pipeline/evals/recorder.py` (recorder seam) — the 3.8 remapper (§3.3).
- `.planning/elevation/UPGRADES.html` — the living tracker (keep current). `.planning/elevation/IMPLEMENTATION-PLAN.md` — Wave-5 change-ids + full roadmap. `.planning/elevation/AUDIT.md`.
- `core/pipeline/` + `pipeline/stages.py` — the 3.7 runner (context for the eventual 3.7 flip).

## 7. Test data + how to verify
- Suite: `venv/bin/pytest tests/ -q -p no:warnings` → 554 passed / 1 skipped (offline; LLM stubbed/replayed). Use `--junit-xml` + parse for exact counts.
- venv: `venv/bin/python` (3.11.15); `instructor`, `litellm`, `pydantic` v2, `sentence-transformers` (MiniLM).
- If reads EPERM → macOS Full Disk Access (§4).

## 8. Documents to read in order
1. This handoff. 2. `HANDOFF_2026-06-22_PART_2.md` (offline backlog + 3.7 build detail). 3. `.planning/elevation/UPGRADES.html` (roadmap "Next up" / "Planned") + `IMPLEMENTATION-PLAN.md` (Wave-5 change-ids). 4. Memory: `elevation-progress`, `inv4-cassette-dedup-uuid`, `keep-upgrades-tracker-updated`, `graphify-refresh-method`.

## 9. How to verify "done" next session
- [ ] Wave-5 Jira issue-link direction fixed (test asserts correct inward/outward); suite green.
- [ ] Wave-5 typed exception classification landed; suite green.
- [ ] 3.8 replay-time dedup-UUID remapper built + unit-tested against synthetic cassettes; suite green.
- [ ] `UPGRADES.html` updated after EACH increment (cards + metrics + date); `graphify update .` each time.
- [ ] Suite ≥ 554 green; adversarial-review → 0 confirmed defects per increment; commits made; nothing pushed without go-ahead; Linear deferred; STEP 5.1 / llm_router / BIFROST untouched.
