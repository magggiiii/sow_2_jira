---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase11_evals.py
  - infra/admin/evaluator/Dockerfile
  - infra/admin/evaluator/requirements.txt
  - infra/admin/docker-compose.admin.yml
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "User container performance is not affected by evaluation traffic"
    - "Evaluator Docker container is built and integrated"
  artifacts:
    - path: "infra/admin/evaluator/Dockerfile"
      provides: "Evaluation service container"
    - path: "tests/test_phase11_evals.py"
      provides: "Validation tests for evaluations scaffold"
  key_links:
    - from: "infra/admin/docker-compose.admin.yml"
      to: "s2j-admin-evaluator"
      via: "docker compose service"
---

<objective>
Implement the Evals Architecture infrastructure to run offline evaluations in the admin container.

Purpose: To automatically score production traces using LLM-as-a-judge for precision/recall without adding latency to the user extraction pipeline.
Output: Evaluator Docker container and integration into admin compose stack.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-evals-architecture/11-CONTEXT.md
@.planning/phases/11-evals-architecture/11-RESEARCH.md
@.planning/phases/11-evals-architecture/11-VALIDATION.md
@infra/admin/docker-compose.admin.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Evals Tests and Evaluator Image (Wave 0)</name>
  <files>tests/test_phase11_evals.py, infra/admin/evaluator/Dockerfile, infra/admin/evaluator/requirements.txt</files>
  <read_first>.planning/phases/11-evals-architecture/11-VALIDATION.md</read_first>
  <action>
Create test scaffold and initial evaluator environment.

1. Create `tests/test_phase11_evals.py` with three test stubs:
   - `def test_dataset_integrity():` (assert dataset 'sow-extraction-full-ground-truth' exists)
   - `def test_evaluator_execution():` (placeholder for evaluator run)
   - `def test_score_propagation():` (placeholder for score push)

2. Create `infra/admin/evaluator/requirements.txt` with exact values:
   `langfuse`
   `langchain`
   `langchain-openai`
   `schedule`
   `pydantic`
   `pytest`

3. Create `infra/admin/evaluator/Dockerfile` with exact values:
   `FROM python:3.11-slim`
   `WORKDIR /app`
   `COPY requirements.txt .`
   `RUN pip install -r requirements.txt`
   `COPY . .`
   `CMD ["python", "main.py"]`
  </action>
  <verify>
    <automated>pytest tests/test_phase11_evals.py --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def test_dataset_integrity" tests/test_phase11_evals.py` returns 0.
    - `grep "langchain-openai" infra/admin/evaluator/requirements.txt` returns 0.
    - `grep "FROM python:3.11-slim" infra/admin/evaluator/Dockerfile` returns 0.
  </acceptance_criteria>
  <done>Tests are scaffolded and Dockerfile/requirements are created</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Evaluator Service into Admin Compose</name>
  <files>infra/admin/docker-compose.admin.yml</files>
  <read_first>infra/admin/docker-compose.admin.yml</read_first>
  <action>
Add the evaluator service to the admin stack to run alongside Langfuse and Bifrost.

Modify `infra/admin/docker-compose.admin.yml` to append a new service named `evaluator` under the `services:` block:
```yaml
  evaluator:
    build: ./evaluator
    container_name: s2j-admin-evaluator
    environment:
      - LANGFUSE_HOST=http://langfuse:3000
      - LANGFUSE_PUBLIC_KEY=pk-lf-1234567890
      - LANGFUSE_SECRET_KEY=sk-lf-1234567890
      - OPENAI_API_BASE=http://bifrost:8080/v1
      - OPENAI_API_KEY=bifrost-admin-key
      - EVAL_INTERVAL_MINUTES=60
    depends_on:
      - langfuse
      - bifrost
    networks:
      - argus-network
    restart: always
```
  </action>
  <verify>
    <automated>docker compose -f infra/admin/docker-compose.admin.yml config > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep "container_name: s2j-admin-evaluator" infra/admin/docker-compose.admin.yml` returns 0.
    - `grep "OPENAI_API_BASE=http://bifrost:8080/v1" infra/admin/docker-compose.admin.yml` returns 0.
    - `grep "LANGFUSE_HOST=http://langfuse:3000" infra/admin/docker-compose.admin.yml` returns 0.
  </acceptance_criteria>
  <done>Evaluator service is fully configured in the compose file</done>
</task>

</tasks>

<verification>
- Verify Dockerfile and requirements exist and are valid
- Verify admin compose configuration has no errors
</verification>

<success_criteria>
- The evaluator runs as a dedicated service via `docker-compose.admin.yml`
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-01-SUMMARY.md`
</output>