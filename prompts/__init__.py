# prompts/ — versioned prompt registry (STEP 3.6).
#
# The six extraction-pipeline agents source their system/user prompts from the
# byte-faithful ``*.v1.txt`` files in this directory via ``prompts.registry``,
# instead of holding the literals inline. See prompts/registry.py.
