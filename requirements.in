opendataloader-pdf==2.0.2
fastapi==0.135.2
uvicorn==0.42.0
gunicorn==25.3.0
jira==3.10.5
python-dotenv==1.1.0
cryptography==46.0.5
sentence-transformers==5.3.0
scikit-learn==1.8.0
requests==2.32.5
rich==14.3.3
numpy==2.4.3
openai==2.29.0
pydantic==2.12.5
pyyaml==6.0.2
python-multipart==0.0.22
loguru==0.7.3
httpx==0.28.1
pytest-asyncio==1.3.0
# PageIndex dependencies
litellm==1.82.6
pymupdf==1.26.4
pypdf==6.14.2
pytest==9.0.2
instructor==1.15.1
# Langfuse fail-open LLM tracing (WAVE 4, 4.1d) — optional at runtime; the
# llm_client wrapper degrades to a no-op when it's absent/unconfigured.
langfuse==4.0.6
# Async DB engine + ORM + migrations (elevation WAVE 1, STEP 1.2)
sqlalchemy==2.0.51
alembic==1.18.5
asyncpg==0.31.0
# SQLite driver + greenlet bridge for offline async ORM tests (WAVE 2, W2-S0)
aiosqlite==0.22.1
greenlet==3.5.3
# Config layer — pydantic-settings drives the APP_ENV flip (WAVE 2 config layer,
# ENV-CONFIG.md). Was installed ad-hoc but missing from the lockfile.
pydantic-settings==2.13.1
# Sync Postgres driver for the sync SessionStore + Alembic migrations (WAVE 2)
psycopg2-binary==2.9.12
# Async job queue + Redis client (WAVE 2 STEP 2.3 worker seam)
arq==0.28.0
redis==5.3.1
# Google OIDC + signed cookies (WAVE 2 STEP 2.5 auth)
authlib==1.7.2
itsdangerous==2.2.0
# Object storage — Supabase/S3 (boto3) adapter (WAVE 2 config layer / STEP 1.4)
boto3==1.43.42
# pgvector cross-run embeddings, (user_id, project_key)-isolated (WAVE 3 STEP 3.9)
pgvector==0.5.0
