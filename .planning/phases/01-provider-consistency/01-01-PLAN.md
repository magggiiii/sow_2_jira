---
phase: 01-provider-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - test_settings.py
  - requirements.txt
autonomous: true
requirements:
  - PROV-02
  - PROV-03
must_haves:
  truths:
    - "System loads and uses stored credentials without re-prompting"
    - "System displays clear error when settings are corrupted"
  artifacts:
    - path: "config/settings.py"
      provides: "SettingsManager class"
  key_links:
    - from: "config/settings.py"
      to: "data/settings.json"
      via: "SettingsManager.load()"
      pattern: "json.load"
---

<objective>
Centralize settings logic and model formatting.

Purpose: Eliminate duplicated Fernet encryption logic and ensure consistent model string formatting across the system. Fail fast if settings are corrupted.
Output: A robust `config/settings.py` module with unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add pytest to dependencies</name>
  <files>
    - requirements.txt
  </files>
  <action>
    Add `pytest>=8.0.0` to `requirements.txt`. (Do not remove existing lines).
  </action>
  <acceptance_criteria>
    - `requirements.txt contains pytest>=8.0.0`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "pytest" requirements.txt</automated>
  </verify>
  <done>pytest is added to requirements.txt</done>
</task>

<task type="auto">
  <name>Task 1: Create centralized SettingsManager</name>
  <read_first>
    - ui/server.py
  </read_first>
  <files>
    - config/settings.py
  </files>
  <action>
    Create `config/settings.py`. Implement class `SettingsManager`.
    Add `__init__(self, data_dir: str = "data")`. 
    Move Fernet key derivation and loading logic from `ui/server.py` (`_derive_fernet_key`, `_load_or_create_fernet_key`, `_encrypt_secret`, `_decrypt_secret`) into `SettingsManager` methods. Read from `SOW_FERNET_KEY` env var or `data/.keyfile`.
    Implement `load(self) -> dict` to read `data/settings.json`. Include the exact default initialization logic found in `ui/server.py`'s `_load_settings()` (if "providers" not in data...).
    Crucially, if the file exists but `json.load()` fails, raise `RuntimeError("Corrupted settings.json")` (do not catch and return `{}`).
    Implement `save(self, data: dict) -> None`.
  </action>
  <acceptance_criteria>
    - `config/settings.py contains class SettingsManager`
    - `config/settings.py contains raise RuntimeError`
    - `config/settings.py does not contain except Exception: return {}` (for file reading)
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile config/settings.py</automated>
  </verify>
  <done>SettingsManager is implemented and fails fast on invalid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Registry and Formatting Logic</name>
  <read_first>
    - ui/server.py
  </read_first>
  <files>
    - config/settings.py
  </files>
  <action>
    In `config/settings.py`, add `PROVIDER_REGISTRY` dictionary exactly as found in `ui/server.py` (lines 145-156).
    Add `def resolve_provider_base(provider: str, base_url: Optional[str]) -> Optional[str]:` copying logic from `ui/server.py`.
    Add `def build_litellm_model(provider: str, model: Optional[str], azure_deployment: Optional[str]) -> str:` copying the logic exactly from `ui/server.py`'s `_build_litellm_model`.
  </action>
  <acceptance_criteria>
    - `config/settings.py contains PROVIDER_REGISTRY = {`
    - `config/settings.py contains def build_litellm_model`
    - `config/settings.py contains def resolve_provider_base`
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile config/settings.py</automated>
  </verify>
  <done>Formatting functions and registry are moved to settings module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit tests for settings</name>
  <read_first>
    - config/settings.py
  </read_first>
  <files>
    - test_settings.py
  </files>
  <behavior>
    - Test 1: Calling load() on a malformed settings.json raises RuntimeError.
    - Test 2: build_litellm_model("google", "gemini-1.5", None) == "gemini/gemini-1.5"
    - Test 3: build_litellm_model("ollama", "qwen2.5:7b", None) == "ollama/qwen2.5:7b"
  </behavior>
  <action>
    Create `test_settings.py` at the repository root. Implement tests for `SettingsManager` using `pytest`. Write invalid JSON to a temporary file and verify `SettingsManager(data_dir=tmp_path).load()` raises `RuntimeError`. Test `build_litellm_model` for various providers.
  </action>
  <acceptance_criteria>
    - `test_settings.py contains def test_corrupted_settings_throws_error`
    - `test_settings.py contains def test_build_litellm_model`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest test_settings.py</automated>
  </verify>
  <done>Unit tests created and passing.</done>
</task>

</tasks>

<verification>
Check that `config/settings.py` is self-contained and handles errors.
</verification>

<success_criteria>
- `SettingsManager` loads and saves settings without silent corruption.
- Formatting functions are extracted.
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-consistency/01-01-SUMMARY.md`
</output>