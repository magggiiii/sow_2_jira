# Handoff — Elevation: Argus decommissioned + tracker reconciled → execute WAVE 7 (offline completion)

**Generated:** 2026-07-07
**Branch:** `elevation/wave-0` (141 commits ahead of `main`; content pushed → GitHub origin + GitLab through `9929ad3`)
**Checkpoint commit:** the `docs: session checkpoint 2026-07-07` commit at branch HEAD (this handoff) — **local-only until you push it**; see `git log -1 --oneline`.
**Linear:** none — this elevation is tracked via `.planning/elevation/` + `UPGRADES.html` + project memory, not a CAL ticket.
**Status:** §8 Argus decommission DONE + pushed; all 9 `UPGRADES.html` wave bars reconciled against code; the next offline max-parallel wave (**WAVE 7**) is planned and ready to execute (`NEXT-SESSION-PLAN.md §9`).

## 1. What's done this session
| Item | Status |
|---|---|
| **§8 Argus decommission** (AR-OBS `2d9715d` + AR-INFRA-DOCS `4e6f247`) | DONE + pushed. observability.py fully de-Argus/de-OTel (Langfuse SDK + no-op shims kept); infra/user + argus-collector-edge.yaml deleted; tempo.yaml → config/admin; install.sh + README/CLAUDE/AGENTS scrubbed. |
| Argus verified unneeded | 10-agent audit, HIGH confidence — Render/Vercel logs + Langfuse Cloud + Bifrost cover every live signal; OTel plane already emitted nothing. |
| **Tracker reconciliation** (`f619767`, `2725020`, `72a5304`) | All 9 wave bars re-baselined against code (9-track audit, HIGH confidence). Wave 6 synced (UI.1→UI.7 shipped). |
| **§9 WAVE 7 plan** (`9929ad3`) | 11-agent planner (HIGH confidence). Ready to execute. |
| Green baseline | pytest **831/0/2**, vitest **96/96**, OTel-absent import OK, leaked-token-in-tracked-files = 0. |
| LOCKED untouched | llm_router/configure_litellm_for_mode, BIFROST_*/bifrost.admin.yaml/infra-admin, data/.keyfile, os.environ creds, OLLAMA_*/STEP 5.1. |

**Reconciliation corrections applied to bars:** Wave 0 Done→85% (PyPDF2/lockfile Render-blocker open), Wave 2 70%→55% (no worker; still in-process), Wave 4 85%→78% (3-service render.yaml, telemetry call-sites remain, no per-user cost); Wave 3 82%→92%, Wave 5 58%→90% (underclaims). Wave 1/6/Harness/Scale accurate.

## 2. Branch state (commits this session, newest first)
```
<this checkpoint doc commit — "docs: session checkpoint 2026-07-07" — sits on top, local-only>
9929ad3 docs(elevation): plan §9 WAVE 7 — next offline max-parallel wave
72a5304 docs(elevation): UPGRADES — reconcile all 9 wave bars against code (9-track audit)
2725020 docs(elevation): UPGRADES — sync tracker to shipped code (Wave 6 UI.1→UI.7 done)
f619767 docs(elevation): UPGRADES — Final wave, Argus fleet decommissioned
4e6f247 chore(elevation): AR-INFRA-DOCS — delete Argus fleet infra/config/installer/doc footprint
2d9715d refactor(elevation): AR-OBS — de-Argus + fully de-OTel observability.py; keep Langfuse SDK + shims
```
All 6 pushed to both remotes (verified `git ls-remote`). Prior session ended at `c35a8e5`.

## 3. The next session's mission — execute WAVE 7 (spec: `NEXT-SESSION-PLAN.md §9`)
Same cadence: **TDD → adversarial-review Workflow (3 default-reject skeptics + adjudication) → explicit-path commit**; UPGRADES.html + `graphify update .` run **once** at the R3 gate.

**Resolve the 3 OPEN DECISIONS first (§9):**
1. Lockfile tool — recommend **uv** (installed, hash-locking).
2. **infr-15c go-ahead (LOCKED-adjacent):** confirm deleting the 9 no-op `tracer.start_as_current_span` sites + 2 `sync_telemetry()` calls (observability.py shim DEFINITIONS preserved; `llm_client.py:424` callback wiring untouched). If declined, drop infr-15c — rest of Wave 7 unaffected.
3. Cost-gate token source — recommend a run-scoped `(prompt,completion)` accumulator threaded from `llm_client` (NOT a mid-run `audit.db` read).

**Then R1 (5 file-disjoint agents) → R2 (schemas merge) → R3 (integration gate):**
- **A · SC-DEPS** (M): `pip install pypdf` first → `requirements.in`✚ (seed from `pip freeze`) → `requirements.txt` regen `uv pip compile --generate-hashes` **pinned-to-installed** → `pageindex/utils.py` PyPDF2→pypdf (:8 + 6 PdfReader sites + :830-831; **NOT :31**) → `Makefile` → `tests/test_deps_pypdf_parity.py`✚.
- **B · SC-DEPLOY** (M): `render.yaml` 3→4-service (worker/cron→existing `scripts/gc_sessions.py`/envVarGroups/preDeployCommand) + `pipeline/telemetry.py:10` denylist→allowlist (superset of ~20 keys incl. `cost_usd`) + 2 tests.
- **C · SC-TESTS** (S): `tests/test_audit_logger.py`✚ (zero prod change; 9-col schema; exclude run_id back-compat cases owned by `test_audit_logger_run_id.py`).
- **D · SC-SCHEMA** (M) — **SOLE R1 owner of `models/schemas.py`**: SourceRef :289 RAISING validators (page_end≥page_start, depth≥0; NOT `Field(ge=)`) + `models/intelligence_schemas.py`✚ (8 moved schemas + re-exports in coverage_check/critic/classifier/judges) + `models/eval_schemas.py` GoldenTicket AC retype→`list[AcceptanceCriterion]` + 3 tests.
- **E · SC-ORCH + infr-15c** (M) — **SOLE HOT owner** of `orchestrator.py`+`deduplication.py`+`core/cost_meter.py`✚+`jira_client.py`+`llm_client.py`: `dedup_against(recovered, existing)` (swap the full re-dedup at orchestrator.py:1085-1086) + CostMeter kill-switch (gate at `_cancelled()` checkpoints :798/:824/:853; `litellm.cost_per_token`, never `completion_cost`) + infr-15c call-site cleanup. Reads `RunConfig.max_run_cost` via `getattr(...)`; **defers the schemas.py field to R2**.
- **R2 · SCHEMA-MERGE:** append `RunConfig.max_run_cost` + `>0` validator to `models/schemas.py`.
- **R3 · INTEGRATION:** `pytest` ∥ `make verify` ∥ `ruff check .` → then UPGRADES.html (bump Wave 0/3/5 bars) + `graphify update .` once.

## 4. Open behavioral risks to watch
- **uv drift:** offline `uv pip compile` warm-cache resolves a *drifted* set (fastapi 0.135.1 vs installed 0.135.2, litellm 1.82.4 vs 1.82.6, starlette 0.52.1 vs 1.0.0). MUST seed `requirements.in` from `pip freeze` and pin to the *installed* venv + add a drift-gate assert, else GREEN validates a drifted baseline.
- **pypdf parity:** `pypdf.PdfReader`/`.pages`/`.extract_text()`/`.metadata.title` are drop-in for PyPDF2, but verify against a synthesized in-memory PDF (don't assume `data/` sample PDFs exist — gitignored).
- **schemas.py ordering:** D (SourceRef, R1) then F (RunConfig.max_run_cost, R2) — one writer per round or they cross-stage.
- **infr-15c scope:** collapse is span-only. **`llm_client.py:424` `if SYNC_ENABLED: litellm.success_callback=['opentelemetry']` is BEHAVIOR, not a span — do NOT remove it.** observability.py shim DEFINITIONS stay.
- **Local venv has OTel installed + `.env` sets Langfuse keys** → local runs exercise the real OTel path, not the shims. Validate de-OTel against a clean-deps build.

## 5. Explicitly NOT in scope (locked)
- **LOCKED (never touch):** `llm_router`/`configure_litellm_for_mode`, `BIFROST_*`/`bifrost.admin.yaml`/`infra/admin`, `data/.keyfile`, `os.environ` JIRA_*/LITELLM_* credential writes (= STEP 2.4), `OLLAMA_*`/STEP 5.1, `pageindex/utils.py:31` CHATGPT→OPENAI alias.
- **WALLED (needs live services — don't attempt):** `pipeline/worker.py` (arq/redis), real Render deploy, per-user cost aggregation/persistence, STEP 2.4/2.2 credential injection, faithful 103-node cassette (live gemini), real Postgres/R2 audit asserts, browser smoke, live Langfuse trace.

## 6. Key files for the deep dive
- `.planning/elevation/NEXT-SESSION-PLAN.md` **§9** — the executable WAVE 7 spec (streams/rounds/conflict-map/verification/DAG/open-decisions).
- `.planning/elevation/UPGRADES.html` — living tracker (bars now code-accurate; "Reconciliation note" lists open-behind-increment items).
- `pipeline/orchestrator.py` — HOT file for agent E (dedup :1085-1086, `_cancelled()` :798/:824/:853, sync_telemetry :919/:1234, 7 spans).
- `models/schemas.py` (SourceRef :289) · `models/eval_schemas.py` (GoldenTicket :13) · `pipeline/agents/deduplication.py` (:364 untouched; add `dedup_against`).
- `requirements.txt` (1/30 pinned, PyPDF2:23) · `pageindex/utils.py` (:8 + 6 PdfReader sites).
- `pipeline/telemetry.py:10` (denylist) · `render.yaml` (3-service) · `audit/logger.py` (9-col schema).

## 7. Test data + how to run
- Backend: `PYTHONDONTWRITEBYTECODE=1 venv/bin/pytest tests/ -q --junit-xml=/tmp/x.xml` + parse the XML (rtk eats the summary). **Clear `__pycache__` first** (stale bytecode → false-greens). Baseline 831/0/2.
- Frontend: `npm test` (vitest, 96/96). Import smoke: `make verify`. Lint: `ruff check .`.
- Local creds live in gitignored `.env` (Bifrost + Langfuse keys). Do not commit.

## 8. Documents to read in order
1. This handoff.
2. `.planning/elevation/NEXT-SESSION-PLAN.md` §9 (WAVE 7 spec) — and §8 for the just-finished Argus context.
3. `.planning/elevation/UPGRADES.html` (state at a glance + reconciliation note).
4. Memory: `~/.claude/projects/-Users-magi-Documents-projects-sow-to-jira/memory/elevation-progress.md`.

## 9. How to verify "done" at the end of WAVE 7
- `venv/bin/pytest tests/` green (≥831 + new tests) ∥ `npm test` 96/96 ∥ `make verify` EXIT 0 ∥ `ruff check .` clean.
- `requirements.txt` fully `==`-pinned + hash-locked, `PyPDF2` gone (`git grep -i pypdf2` = 0 tracked), fresh-venv resolves.
- `SourceRef` rejects `page_end<page_start` / `depth<0`; `models/intelligence_schemas.py` imports (all old sites unbroken); GoldenTicket AC = `list[AcceptanceCriterion]` + subset test.
- CostMeter aborts a run over `max_run_cost` via `_cancelled()`; `dedup_against` dedups only recovered vs survivors.
- `render.yaml` is 4-service and validates offline; `pipeline/telemetry.py` allowlist is a superset of live emit keys.
- (if infr-15c approved) `git grep sync_telemetry` / `start_as_current_span` in orchestrator/jira_client/llm_client = 0 call sites; shims still import.
- `UPGRADES.html` Wave 0/3/5 bars bumped to reflect Wave 7; committed + (on go-ahead) pushed.

## Still open (out-of-band / human)
- **Rotate `ARGUS_BACKBONE_TOKEN`** in the Argus backend — scrubbed from tracked files but still in pushed git history (decided: rotate, no history rewrite). The fleet using it is now retired.
- Deferred follow-ups (logged, user-acked): Langfuse flush-on-shutdown hook; `audit.db`→Postgres; set `LANGFUSE_*` as Render secrets; regenerate `.planning/codebase/*` (stale vs shipped flat layout).
