---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase11_evals.py
  - infra/admin/evaluator/Dockerfile
  - infra/admin/evaluator/requirements.txt
  - infra/admin/evaluator/main.py
  - infra/admin/docker-compose.admin.yml
  - scripts/seed_full_langfuse_dataset.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "The evaluation service runs continuously as an isolated background service in the admin stack"
    - "User container performance is not affected by evaluation traffic"
    - "Langfuse dataset 'sow-extraction-full-ground-truth' exists and is populated with ground-truth data"
  artifacts:
    - path: "infra/admin/evaluator/Dockerfile"
      provides: "Evaluation service container"
    - path: "tests/test_phase11_evals.py"
      provides: "Validation tests for evaluations scaffold"
    - path: "scripts/seed_full_langfuse_dataset.py"
      provides: "Dataset seeding script"
  key_links:
    - from: "infra/admin/docker-compose.admin.yml"
      to: "s2j-admin-evaluator"
      via: "docker compose service"
---

<objective>
Implement the Evals Architecture infrastructure and seed the initial ground-truth dataset.

Purpose: To establish the isolated background service for offline evaluations and provide the baseline data needed for scoring.
Output: Evaluator Docker container, admin stack integration, and seeded Langfuse dataset.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-evals-architecture/11-CONTEXT.md
@.planning/phases/11-evals-architecture/11-RESEARCH.md
@.planning/phases/11-evals-architecture/11-VALIDATION.md
@infra/admin/docker-compose.admin.yml
@scripts/seed_full_langfuse_dataset.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Evals Tests and Evaluator Image (Wave 0)</name>
  <files>tests/test_phase11_evals.py, infra/admin/evaluator/Dockerfile, infra/admin/evaluator/requirements.txt, infra/admin/evaluator/main.py</files>
  <read_first>.planning/phases/11-evals-architecture/11-VALIDATION.md</read_first>
  <action>
Create test scaffold, evaluator environment, and a stub entry point.

1. Create `tests/test_phase11_evals.py` with three test stubs:
   - `def test_dataset_integrity():` (assert dataset 'sow-extraction-full-ground-truth' exists)
   - `def test_evaluator_execution():` (placeholder for evaluator run)
   - `def test_score_propagation():` (placeholder for score push)

2. Create `infra/admin/evaluator/requirements.txt`:
   `langfuse`
   `langchain`
   `langchain-openai`
   `schedule`
   `pydantic`
   `pytest`

3. Create `infra/admin/evaluator/Dockerfile`:
   `FROM python:3.11-slim`
   `WORKDIR /app`
   `COPY requirements.txt .`
   `RUN pip install -r requirements.txt`
   `COPY . .`
   `CMD ["python", "main.py"]`

4. Create `infra/admin/evaluator/main.py` (stub):
   `import time; print("Evaluator stub running..."); time.sleep(3600)`
  </action>
  <verify>
    <automated>pip install -r infra/admin/evaluator/requirements.txt && pytest tests/test_phase11_evals.py --collect-only</automated>
  </verify>
  <done>Scaffold files created and local requirements installed for verification</done>
</task>

<task type="auto">
  <name>Task 2: Integrate and Start Evaluator Service in Admin Stack</name>
  <files>infra/admin/docker-compose.admin.yml</files>
  <read_first>infra/admin/docker-compose.admin.yml</read_first>
  <action>
Add the evaluator service to the admin stack and start it.

1. Modify `infra/admin/docker-compose.admin.yml` to append the `evaluator` service under `services:`:
```yaml
  evaluator:
    build: ./evaluator
    container_name: s2j-admin-evaluator
    environment:
      - LANGFUSE_HOST=http://langfuse:3000
      - LANGFUSE_PUBLIC_KEY=pk-lf-1234567890
      - LANGFUSE_SECRET_KEY=sk-lf-1234567890
      - OPENAI_API_BASE=http://bifrost:8080/v1
      - OPENAI_API_KEY=bifrost-admin-key
      - EVAL_INTERVAL_MINUTES=60
    volumes:
      - ./evaluator/main.py:/app/main.py
      - ../../tests:/app/tests
    depends_on:
      - langfuse
      - bifrost
    networks:
      - argus-network
    restart: always
```

2. Start the service: `docker compose -f infra/admin/docker-compose.admin.yml up -d evaluator`
  </action>
  <verify>
    <automated>docker ps --filter "name=s2j-admin-evaluator" --format "{{.Status}}" | grep -i "up"</automated>
  </verify>
  <done>Evaluator service is configured and running as a background service</done>
</task>

<task type="auto">
  <name>Task 3: Seed Ground-Truth Dataset in Langfuse</name>
  <files>scripts/seed_full_langfuse_dataset.py</files>
  <action>
Execute the seed script to ensure the dataset exists.
Run `python scripts/seed_full_langfuse_dataset.py` (ensure LANGFUSE_HOST points to the correct endpoint, likely http://localhost:3002 during execution).
  </action>
  <verify>
    <automated>python -c "import os; os.environ['LANGFUSE_HOST'] = os.getenv('LANGFUSE_HOST', 'http://localhost:3002'); from langfuse import Langfuse; assert Langfuse().get_dataset('sow-extraction-full-ground-truth') is not None, 'Dataset not found'"</automated>
  </verify>
  <done>Langfuse dataset contains ground-truth tickets for all features</done>
</task>

</tasks>

<verification>
- `docker ps` shows `s2j-admin-evaluator` is healthy and running.
- Langfuse API confirms 'sow-extraction-full-ground-truth' dataset exists.
</verification>

<success_criteria>
- The evaluator runs as a dedicated isolated service.
- The ground-truth dataset is seeded and accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-01-SUMMARY.md`
</output>
