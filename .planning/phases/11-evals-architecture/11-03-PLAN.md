---
phase: 11-evals-architecture
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified: [pipeline/evals/judges.py, infra/admin/evaluator/main.py]
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "Evaluation results (scores) appear on Langfuse traces"
    - "Hierarchical comparison logic accounts for EPIC -> Tickets structure"
    - "Local Ollama is used for scoring (zero cost)"
  artifacts:
    - path: "pipeline/evals/judges.py"
      provides: "The core LLM-as-a-judge comparison logic"
    - path: "infra/admin/evaluator/main.py"
      provides: "Batch evaluation loop"
---

<objective>
Implement the core hierarchical comparison logic using LLM-as-a-judge and integrate it into the evaluator's offline batch scoring process. This ensures that every extraction trace is scored for precision, recall, and hierarchical accuracy against the golden dataset without manual intervention.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-evals-architecture/REVIEWS.md
@.planning/phases/11-evals-architecture/11-RESEARCH.md
@models/eval_schemas.py
@infra/admin/evaluator/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hierarchical Judge</name>
  <files>pipeline/evals/judges.py</files>
  <read_first>
    - models/eval_schemas.py (for golden structures)
  </read_first>
  <action>
    Create `pipeline/evals/judges.py` using LangChain:
    1. Define a system prompt for the "Hierarchical Judge" that explains the `EPIC -> Tickets` structure.
    2. Input to judge: `actual_output` (generated by engine) vs `expected_output` (golden set).
    3. Output from judge: JSON containing scores for `Epic Alignment`, `Story Coverage`, `Story Fidelity`, and `Hallucination`.
    4. Configure it to use `ChatOpenAI` pointing to the Admin Bifrost endpoint (Ollama).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_hierarchical_judge.py 2>/dev/null || echo "Test scaffold needed"</automated>
  </verify>
  <acceptance_criteria>
    - Judge returns structured scores (0-1 range).
    - Judge provides brief reasoning for each score.
    - Judge operates correctly on nested JSON hierarchies.
  </acceptance_criteria>
  <done>LLM-as-a-judge implemented with support for hierarchical comparison</done>
</task>

<task type="auto">
  <name>Task 2: Implement Offline Evaluation Loop</name>
  <files>infra/admin/evaluator/main.py</files>
  <read_first>
    - pipeline/evals/judges.py (to import the judge)
  </read_first>
  <action>
    Update `infra/admin/evaluator/main.py` to:
    1. Fetch all traces from Langfuse using the Python SDK.
    2. Filter for traces that are linked to a dataset run or need scoring.
    3. For each trace, fetch the corresponding `expected_output` from the Langfuse dataset.
    4. Pass the trace output and the golden output to the `HierarchicalJudge`.
    5. Push the resulting scores back to Langfuse using `langfuse.score()`.
    6. Ensure the loop runs periodically (batch mode).
  </action>
  <verify>
    <automated>docker compose -f infra/admin/docker-compose.admin.yml restart evaluator && docker compose -f infra/admin/docker-compose.admin.yml logs evaluator</automated>
  </verify>
  <acceptance_criteria>
    - Evaluator process polls Langfuse every N minutes.
    - Successfully scored traces show `Precision`, `Recall`, and `Format` scores in the Langfuse UI.
    - No impact on production user traffic performance.
  </acceptance_criteria>
  <done>Automatic offline evaluation and scoring active in the admin stack</done>
</task>

</tasks>

<verification>
Execute a manual extraction run, then check Langfuse to confirm the evaluator has picked up the trace, performed the hierarchical comparison, and attached the resulting scores.
</verification>

<success_criteria>
- Scoring is fully automated and offline.
- Costs are zero due to local Ollama usage.
- Scores reflect the hierarchical quality of the extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-03-SUMMARY.md`
</output>
