---
phase: 11-evals-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [models/eval_schemas.py, scripts/seed_hierarchical_eval_dataset.py, scripts/run_eval_dataset.py]
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "Langfuse contains a dataset with hierarchical EPIC -> Tickets structure"
    - "Extraction engine can be triggered against the dataset to produce a 'Dataset Run'"
  artifacts:
    - path: "models/eval_schemas.py"
      provides: "Common structure for hierarchical evaluation"
    - path: "scripts/seed_hierarchical_eval_dataset.py"
      provides: "Automation for populating the Golden Dataset"
    - path: "scripts/run_eval_dataset.py"
      provides: "Run logic to execute extraction against the dataset"
---

<objective>
Define and populate the Hierarchical Golden Dataset in Langfuse and implement the run logic to execute the extraction engine against it. This establishes the ground truth and the mechanism for generating comparative results.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-evals-architecture/11-CONTEXT.md
@.planning/phases/11-evals-architecture/REVIEWS.md
@models/schemas.py
@pipeline/orchestrator.py
@scripts/seed_full_langfuse_dataset.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Hierarchical Eval Schemas</name>
  <files>models/eval_schemas.py</files>
  <read_first>
    - models/schemas.py (to reference RawTask and ManagedTask)
  </read_first>
  <action>
    Create `models/eval_schemas.py` defining:
    - `GoldenTicket`: Matches `RawTask` but with strict expected values.
    - `GoldenEpic`: Contains summary, description, and a list of `GoldenTicket` items.
    - `HierarchicalDatasetItem`: The final structure for Langfuse `expected_output` containing the Epic and its Stories.
  </action>
  <verify>
    <automated>python3 -c "from models.eval_schemas import HierarchicalDatasetItem; print('Import successful')"</automated>
  </verify>
  <acceptance_criteria>
    - Schemas are Pydantic-based.
    - Structures support the EPIC -> tickets hierarchy requested in REVIEWS.md.
  </acceptance_criteria>
  <done>Evaluation schemas defined to support hierarchical comparison</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hierarchical Seeding Script</name>
  <files>scripts/seed_hierarchical_eval_dataset.py</files>
  <read_first>
    - scripts/seed_full_langfuse_dataset.py (as a template)
  </read_first>
  <action>
    Create `scripts/seed_hierarchical_eval_dataset.py` to:
    1. Group the 27 SOW features from `seed_full_langfuse_dataset.py` into Logical Epics (e.g., IAM, AP, TM, CR, YE).
    2. Upload these to a new Langfuse dataset named `sow-hierarchical-golden-set`.
    3. The `input` should be the feature description group, and `expected_output` should follow the `HierarchicalDatasetItem` schema.
  </action>
  <verify>
    <automated>python3 scripts/seed_hierarchical_eval_dataset.py</automated>
  </verify>
  <acceptance_criteria>
    - Script successfully pushes data to Langfuse on port 3002.
    - Dataset items in Langfuse UI show the expected nested JSON structure.
  </acceptance_criteria>
  <done>Golden Dataset seeded with hierarchical ground truth</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dataset Run Script</name>
  <files>scripts/run_eval_dataset.py</files>
  <read_first>
    - pipeline/orchestrator.py (to see how to trigger extraction)
  </read_first>
  <action>
    Create `scripts/run_eval_dataset.py` to:
    1. Fetch all items from the `sow-hierarchical-golden-set` dataset in Langfuse.
    2. For each item, initialize an `Orchestrator` (or `ExtractionAgent`) and pass the `input` (SOW text).
    3. Capture the output and link it back to the dataset item as a "Dataset Run" named `eval-run-[timestamp]`.
    4. Ensure the trace is properly tagged for identification by the evaluator loop.
  </action>
  <verify>
    <automated>pytest tests/test_phase11_evals.py::test_run_eval_dataset_script</automated>
  </verify>
  <acceptance_criteria>
    - Script successfully iterates over dataset items.
    - Script creates a new "Dataset Run" in Langfuse.
    - Traces are correctly linked to dataset items.
  </acceptance_criteria>
  <done>Run logic implemented to execute extraction engine against the golden dataset</done>
</task>

</tasks>

<verification>
Log in to Langfuse (http://localhost:3002) and verify the `sow-hierarchical-golden-set` dataset exists. Run `python3 scripts/run_eval_dataset.py` and verify a new "Run" appears in the Datasets tab with linked traces.
</verification>

<success_criteria>
- Dataset contains EPIC -> tickets hierarchy.
- Seeding script is repeatable.
- Every SOW feature is accounted for in a golden Epic.
- Run script produces a traceable Dataset Run in Langfuse.
</success_criteria>

<output>
After completion, create `.planning/phases/11-evals-architecture/11-02-SUMMARY.md`
</output>
