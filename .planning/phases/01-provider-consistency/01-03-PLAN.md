---
phase: 01-provider-consistency
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - requirements.txt
  - ui/server.py
  - tests/test_discovery.py
autonomous: true
requirements:
  - PROV-04
must_haves:
  truths:
    - "Model list loads without blocking UI"
    - "Stale models are cleared"
  artifacts:
    - path: "ui/server.py"
      provides: "get_provider_models route"
  key_links:
    - from: "ui/server.py"
      to: "httpx.AsyncClient"
      via: "get_provider_models"
      pattern: "async with httpx.AsyncClient"
---

<objective>
Refactor UI endpoints to use centralized SettingsManager and implement async model discovery.

Purpose: Prevent blocking the FastAPI main thread during model discovery. Clear stale cache entries.
Output: Fast, non-blocking UI endpoints using httpx.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-provider-consistency/01-02-SUMMARY.md

<interfaces>
From `config/settings.py`:
```python
class SettingsManager:
    def load(self) -> dict: ...
    def save(self, data: dict) -> None: ...
    def encrypt_secret(self, value: str) -> str: ...
    def decrypt_secret(self, value: str) -> str: ...

def build_litellm_model(provider: str, model: Optional[str], azure_deployment: Optional[str]) -> str: ...
def resolve_provider_base(provider: str, base_url: Optional[str]) -> Optional[str]: ...
PROVIDER_REGISTRY: dict
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add httpx to dependencies</name>
  <read_first>
    - requirements.txt
  </read_first>
  <files>
    - requirements.txt
  </files>
  <action>
    Add `httpx>=0.27.0`, `pytest>=8.0.0`, and `pytest-asyncio>=0.23.0` to `requirements.txt`. (Do not remove existing lines).
  </action>
  <acceptance_criteria>
    - `requirements.txt contains httpx>=0.27.0`
    - `requirements.txt contains pytest-asyncio`
  </acceptance_criteria>
  <done>Dependencies updated.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor UI endpoints</name>
  <read_first>
    - ui/server.py
  </read_first>
  <files>
    - ui/server.py
  </files>
  <action>
    In `ui/server.py`, remove duplicate logic (`FERNET`, `_load_settings`, `_save_settings`, `_decrypt_secret`, `_encrypt_secret`, `_build_litellm_model`, `_resolve_provider_base`, `PROVIDER_REGISTRY`). 
    Import them from `config.settings`.
    Update `/api/settings` GET and POST routes to instantiate `SettingsManager` and use `load()`, `save()`, `encrypt_secret`, `decrypt_secret`.
    Remove `_apply_settings_to_env(settings)` calls related to LITELLM. **KEEP** the mutations for `JIRA_SERVER` and `JIRA_API_TOKEN` (since Jira integration isn't refactored yet), but move them inline into the `/api/settings` save route.
    Refactor `get_provider_models` to be `async def` and use `async with httpx.AsyncClient() as client: resp = await client.get(url, headers=headers, timeout=8)`.
    Implement a simple dictionary-based cache: `MODEL_CACHE = {}` storing `provider_id: (timestamp, models_list)`. If cache entry is under 5 minutes old, return it immediately to prevent blocking. Ensure changing the provider or API key bypasses the cache.
  </action>
  <acceptance_criteria>
    - `ui/server.py does not contain def _load_settings`
    - `ui/server.py contains async def get_provider_models`
    - `ui/server.py contains async with httpx.AsyncClient`
    - `ui/server.py contains MODEL_CACHE`
  </acceptance_criteria>
  <done>UI endpoints are async and use unified settings manager.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for async discovery</name>
  <read_first>
    - ui/server.py
  </read_first>
  <files>
    - tests/test_discovery.py
  </files>
  <behavior>
    - Test 1: Async model discovery makes httpx call successfully and returns cached models on second call.
  </behavior>
  <action>
    Create `tests/test_discovery.py`. Write a pytest using `@pytest.mark.asyncio` and `unittest.mock.patch` over `httpx.AsyncClient.get` to simulate an API response for `get_provider_models` and ensure caching works.
  </action>
  <acceptance_criteria>
    - `tests/test_discovery.py contains @pytest.mark.asyncio`
    - `tests/test_discovery.py contains def test_`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_discovery.py</automated>
  </verify>
  <done>Async behavior verified.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/` to ensure settings and discovery tests pass.
</verification>

<success_criteria>
- FastAPI correctly serves non-blocking model queries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-consistency/01-03-SUMMARY.md`
</output>
