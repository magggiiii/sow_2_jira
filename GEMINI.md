# SOW-to-Jira AI Guidelines

## Branching Strategy
- **CRITICAL RULE:** All active development, testing, and fixes MUST happen on the **`test`** branch.
- Changes are then gradually bumped up to `dev` and finally to `main`.
- NEVER push directly to `main`.
