---
phase: 02-runtime-logging-reliability
plan: 03
type: execute
wave: 1
depends_on: ["02-02"]
files_modified: ["pipeline/llm_client.py", "pageindex/utils.py", "tests/test_phase2_runtime_reliability.py"]
autonomous: true
requirements: ["PIPE-02", "PIPE-01", "PIPE-03"]
must_haves:
  truths:
    - Remote/API retries are header-first strict (Retry-After and provider reset hints take precedence)
    - Remote/API retries remain bounded by attempts, elapsed time, and max wait safeguards
    - Local Ollama behavior from 02-02 is preserved and not regressed
    - Cancellation interrupts remote backoff immediately and is never retried
    - Retry loops are observable with low-noise structured events and clear user-facing status
  artifacts:
    - path: "pipeline/llm_client.py"
      provides: "Provider-aware remote retry helpers and bounded remote retry execution"
      contains: "extract_retry_hint"
    - path: "pageindex/utils.py"
      provides: "PageIndex parity for header-first remote retry while preserving local unlimited-wait behavior"
      contains: "is_retryable_remote_error"
    - path: "tests/test_phase2_runtime_reliability.py"
      provides: "Regression tests for remote retry precedence, budgets, cancellation, and local/remote isolation"
      contains: "test_remote_retry_after_header_takes_precedence"
  key_links:
    - from: "pipeline/llm_client.py"
      to: "pageindex/utils.py"
      via: "Shared retry model: hint extraction, wait computation, retryability classification"
      pattern: "retry-after-header"
    - from: "pipeline/llm_client.py"
      to: "tests/test_phase2_runtime_reliability.py"
      via: "Remote retry budget and cancellation behavior enforcement"
      pattern: "Remote LLM retry budget exhausted"
---

<objective>
Implement intelligent remote/API provider retry behavior that is rate-limit-aware and production-safe:
1) honor provider retry hints first (header-first strict),
2) fallback to jittered exponential only when no hint exists,
3) keep remote retries bounded by attempt/elapsed/wait budgets,
4) keep cancellation immediate and terminal during backoff,
5) preserve 02-02 local Ollama unlimited-wait behavior unchanged.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Header-First Remote Retry Core</name>
  <read_first>pipeline/llm_client.py</read_first>
  <files>pipeline/llm_client.py</files>
  <action>
    1. Add/verify provider-aware helper layer:
       - `extract_retry_hint(error_or_response) -> RetryHint | None`
       - `compute_wait_seconds(retry_hint, attempt) -> float`
       - `is_retryable_remote_error(error) -> bool`
    2. Enforce strict precedence:
       - use `Retry-After` (seconds/date) exactly when present,
       - else use provider reset headers,
       - else use jittered exponential fallback.
    3. Retry only transient errors (`429`, `5xx`, timeout/connection resets); fast-fail auth/config/model/provider errors.
  </action>
  <acceptance_criteria>
    - Header hints always override fallback
    - Non-retryable errors never enter retry loop
    - Helper outputs are deterministic and bounded by max wait
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remote Budget + Cancellation Safety</name>
  <read_first>pipeline/llm_client.py</read_first>
  <files>pipeline/llm_client.py</files>
  <action>
    1. Enforce remote budgets via env defaults:
       - `LLM_REMOTE_MAX_ATTEMPTS=8`
       - `LLM_REMOTE_MAX_ELAPSED_S=300`
       - `LLM_REMOTE_MAX_WAIT_S=300`
    2. Ensure all backoff waits are cancellation-aware (slice checks) and cancellation is terminal.
    3. Keep local Ollama branch unlimited-wait and isolate from remote budgets.
  </action>
  <acceptance_criteria>
    - Remote loop exits with terminal error when budget is exhausted
    - Cancel signal interrupts wait immediately and prevents next retry
    - Local mode is unaffected by remote budget enforcement
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PageIndex Remote Policy Parity</name>
  <read_first>pageindex/utils.py</read_first>
  <files>pageindex/utils.py</files>
  <action>
    1. Apply same header-first strict remote policy in `llm_completion` and `llm_acompletion`.
    2. Preserve local Ollama unlimited-wait behavior from 02-02 with explicit user messaging.
    3. Keep cancellation-aware backoff for remote and local loops.
  </action>
  <acceptance_criteria>
    - PageIndex remote behavior matches main LLM client policy
    - Local Ollama path remains indefinite wait + transparent status
    - No retry spam tracebacks in steady retry loops
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Observability + Regression Tests</name>
  <read_first>tests/</read_first>
  <files>tests/test_phase2_runtime_reliability.py</files>
  <action>
    1. Add tests for:
       - header-first precedence (`Retry-After`),
       - fallback when no provider hint exists,
       - retryability classification (`429`/`503`/timeouts retry; `401/403` fast-fail),
       - budget enforcement terminal error,
       - cancellation interrupt during backoff,
       - local vs remote isolation,
       - status message includes provider/rate-limit reason and wait source.
    2. Run targeted phase tests and fix regressions.
  </action>
  <acceptance_criteria>
    - New tests pass and prove behavior under both local and remote paths
    - Existing routing tests remain green
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `./venv/bin/pytest tests/test_phase2_runtime_reliability.py -q`.
2. Run `./venv/bin/pytest tests/test_routing.py -q`.
3. Spot-check extraction logs for one remote retry cycle:
   - status mentions provider rate-limit wait reason/source
   - one structured retry event per attempt
   - no repeated traceback spam for expected retries.
</verification>

<success_criteria>
1. Remote retries are intelligent, bounded, and provider-hint driven.
2. Cancellation remains immediate and terminal.
3. Local unlimited-wait behavior remains intact from 02-02.
4. PIPE-02 is satisfied without regressions to PIPE-01 and PIPE-03.
</success_criteria>
